{%- for import in record.imports %}
use super::{{ import.module }}::{{ import.name }};
{%- endfor %}

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct {{ record.pascal_name }} {
{%- for field in record.fields %}
{%- if field.comment %}
    /// {{ field.comment }}
{%- endif %}
    pub {{ field.rust_name }}: {{ field.rust_type }},
{%- endfor %}
}

impl super::runtime::SoraDecode for {{ record.pascal_name }} {
    fn decode(reader: &mut super::runtime::SoraReader<'_>) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
{%- for field in record.fields %}
            {{ field.rust_name }}: <{{ field.rust_type }} as super::runtime::SoraDecode>::decode(reader)?,
{%- endfor %}
        })
    }
}

impl std::fmt::Display for {{ record.pascal_name }} {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut builder = f.debug_struct("{{ record.pascal_name }}");
{%- for field in record.fields %}
        builder.field("{{ field.rust_name }}", &self.{{ field.rust_name }});
{%- endfor %}
        builder.finish()
    }
}
