// This file was generated by Sora. Do not edit manually.

#nullable enable

using System;
using System.Collections.Generic;

namespace com.sora.showcase;

public sealed record ComplexRule(
    int Id,
    string Name,
    // Single union value derived from a tagged_columns child row
    EventCondition RootCondition,
    int RootActionGroup,
    // Non-JSON list<union<RewardAction>> assembled from child rows
    List<RewardAction> Actions,
    // Nested tuple, tuple_list, split, and map parsers in one cell
    ComplexBudget Budget
)
{
    internal static ComplexRule Decode(SoraReader reader)
    {
        return new ComplexRule(
            reader.ReadInt32(),
            reader.ReadString(),
            EventCondition.Decode(reader),
            reader.ReadInt32(),
            reader.ReadList(() => RewardAction.Decode(reader)),
            ComplexBudget.Decode(reader)
        );
    }

    internal static ComplexRule Decode(SoraValue value)
    {
        var obj = value.AsObject("ComplexRule");
        return new ComplexRule(
            obj.Get("id").AsInt32(),
            obj.Get("name").AsString(),
            EventCondition.Decode(obj.Get("root_condition")),
            obj.Get("root_action_group").AsInt32(),
            obj.Get("actions").AsList(item => RewardAction.Decode(item)),
            ComplexBudget.Decode(obj.Get("budget"))
        );
    }

    internal void CollectTextKeys(List<TextKey> keys)
    {
        EventCondition.CollectTextKeys(this.RootCondition, keys);
        foreach (var element in this.Actions)
        {
            RewardAction.CollectTextKeys(element, keys);
        }
        this.Budget.CollectTextKeys(keys);
    }
}

public sealed class ComplexRuleTable : ISoraKeyedTable<int, ComplexRule>
{
    public const string TableName = "ComplexRule";
    public static readonly SoraTableInfo TableInfo = new(
        TableName,
        "ComplexRule",
        SoraTableShape.Keyed,
        new SoraKeyInfo("id", "int"),
        new SoraIndexInfo[]
        {
        }
    );
    private readonly List<int> keys;
    private readonly Dictionary<int, ComplexRule> rows;

    internal ComplexRuleTable(List<int> keys, Dictionary<int, ComplexRule> rows)
    {
        this.keys = keys;
        this.rows = rows;
    }

    internal static ComplexRuleTable Decode(ISoraTableSource source)
    {
        return FromRows(source.DecodeTable(TableName, global::com.sora.showcase.ComplexRule.Decode, global::com.sora.showcase.ComplexRule.Decode));
    }

    internal static ComplexRuleTable FromRows(List<ComplexRule> rows)
    {
        return new ComplexRuleTable(rows.ConvertAll(row => row.Id), SoraConfig.DecodeMapTable(rows, row => row.Id));
    }

    public Dictionary<int, ComplexRule> Rows => rows;
    public ComplexRule this[int key] => rows[key];

    public ComplexRule? Get(int key)
    {
        return rows.TryGetValue(key, out var row) ? row : default;
    }

    public IReadOnlyList<int> OrderedKeys => keys;

    public IEnumerable<int> Keys => keys;

    public IEnumerable<ComplexRule> Values => rows.Values;

    public bool ContainsKey(int key)
    {
        return rows.ContainsKey(key);
    }

    public bool TryGetValue(int key, out ComplexRule value)
    {
        if (rows.TryGetValue(key, out var row))
        {
            value = row;
            return true;
        }
        value = default!;
        return false;
    }

    public IEnumerator<KeyValuePair<int, ComplexRule>> GetEnumerator()
    {
        return rows.GetEnumerator();
    }

    System.Collections.IEnumerator System.Collections.IEnumerable.GetEnumerator()
    {
        return GetEnumerator();
    }

    public IReadOnlyList<ComplexRule> OrderedRows
    {
        get
        {
            var orderedRows = new List<ComplexRule>(keys.Count);
            foreach (var key in keys)
            {
                if (rows.TryGetValue(key, out var row))
                {
                    orderedRows.Add(row);
                }
            }
            return orderedRows;
        }
    }
    public SoraTableInfo Info => TableInfo;
    public int Count => rows.Count;
}
