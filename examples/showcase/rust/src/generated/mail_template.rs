// This file was generated by Sora. Do not edit manually.
use super::SoraMap;

#[repr(transparent)]
#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, serde::Serialize, serde::Deserialize,
)]
#[serde(transparent)]
pub struct MailTemplateId(i32);

impl MailTemplateId {
    pub const fn from_raw(raw: i32) -> Self {
        Self(raw)
    }

    pub const fn raw(self) -> i32 {
        self.0
    }
}

impl From<i32> for MailTemplateId {
    fn from(raw: i32) -> Self {
        Self::from_raw(raw)
    }
}

impl super::runtime::SoraDecode for MailTemplateId {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self::from_raw(<i32 as super::runtime::SoraDecode>::decode(
            reader,
        )?))
    }
}

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct MailTemplate {
    pub id: MailTemplateId,
    pub mail_type: super::mail_type::MailType,
    pub title_key: super::runtime::TextKey,
    pub body_key: super::runtime::TextKey,
    pub rewards: Vec<super::reward::Reward>,
}

impl super::runtime::SoraDecode for MailTemplate {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            id: <MailTemplateId as super::runtime::SoraDecode>::decode(reader)?,
            mail_type: <super::mail_type::MailType as super::runtime::SoraDecode>::decode(reader)?,
            title_key: <super::runtime::TextKey as super::runtime::SoraDecode>::decode(reader)?,
            body_key: <super::runtime::TextKey as super::runtime::SoraDecode>::decode(reader)?,
            rewards: {
                let len = reader.read_var_u32()? as usize;
                let mut values = Vec::with_capacity(len);
                for _ in 0..len {
                    values.push(
                        <super::reward::Reward as super::runtime::SoraDecode>::decode(reader)?,
                    );
                }
                values
            },
        })
    }
}

impl MailTemplate {
    pub(crate) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
        out.push(&self.title_key);
        out.push(&self.body_key);
        for value in self.rewards.iter() {
            value.collect_text_keys(out);
        }
    }
}

#[derive(Debug, Clone)]
pub struct MailTemplateTable {
    keys: Vec<MailTemplateId>,
    rows: SoraMap<MailTemplateId, MailTemplate>,
}

impl MailTemplateTable {
    pub const NAME: &'static str = "MailTemplate";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "MailTemplate",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "id",
            ty: "MailTemplateId",
        }),
        indexes: &[],
    };

    pub(crate) fn from_rows(
        rows: Vec<MailTemplate>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.id).collect::<Vec<_>>();
        Ok(Self {
            keys,
            rows: super::decode_map_table(rows, |row| row.id),
        })
    }

    pub fn get(&self, key: &MailTemplateId) -> Option<&MailTemplate> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[MailTemplateId] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &MailTemplate> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for MailTemplateTable {
    type Target = SoraMap<MailTemplateId, MailTemplate>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for MailTemplateTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for MailTemplateTable {
    type Key = MailTemplateId;
    type Row = MailTemplate;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
