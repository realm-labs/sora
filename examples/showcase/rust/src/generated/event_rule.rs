// This file was generated by Sora. Do not edit manually.
use super::SoraMap;

#[repr(transparent)]
#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, serde::Serialize, serde::Deserialize,
)]
#[serde(transparent)]
pub struct EventRuleId(i32);

impl EventRuleId {
    pub const fn from_raw(raw: i32) -> Self {
        Self(raw)
    }

    pub const fn raw(self) -> i32 {
        self.0
    }
}

impl From<i32> for EventRuleId {
    fn from(raw: i32) -> Self {
        Self::from_raw(raw)
    }
}

impl super::runtime::SoraDecode for EventRuleId {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self::from_raw(<i32 as super::runtime::SoraDecode>::decode(
            reader,
        )?))
    }
}

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct EventRule {
    pub id: EventRuleId,
    pub name: std::sync::Arc<str>,
    pub condition: super::event_condition::EventCondition,
    pub actions: Vec<super::reward_action::RewardAction>,
}

impl super::runtime::SoraDecode for EventRule {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            id: <EventRuleId as super::runtime::SoraDecode>::decode(reader)?,
            name: <std::sync::Arc<str> as super::runtime::SoraDecode>::decode(reader)?,
            condition:
                <super::event_condition::EventCondition as super::runtime::SoraDecode>::decode(
                    reader,
                )?,
            actions: {
                let len = reader.read_var_u32()? as usize;
                let mut values = Vec::with_capacity(len);
                for _ in 0..len {
                    values.push(
                        <super::reward_action::RewardAction as super::runtime::SoraDecode>::decode(
                            reader,
                        )?,
                    );
                }
                values
            },
        })
    }
}

impl EventRule {
    pub(crate) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
        self.condition.collect_text_keys(out);
        for value in self.actions.iter() {
            value.collect_text_keys(out);
        }
    }
}

#[derive(Debug, Clone)]
pub struct EventRuleTable {
    keys: Vec<EventRuleId>,
    rows: SoraMap<EventRuleId, EventRule>,
}

impl EventRuleTable {
    pub const NAME: &'static str = "EventRule";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "EventRule",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "id",
            ty: "EventRuleId",
        }),
        indexes: &[],
    };

    pub(crate) fn from_rows(rows: Vec<EventRule>) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.id).collect::<Vec<_>>();
        Ok(Self {
            keys,
            rows: super::decode_map_table(rows, |row| row.id),
        })
    }

    pub fn get(&self, key: &EventRuleId) -> Option<&EventRule> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[EventRuleId] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &EventRule> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for EventRuleTable {
    type Target = SoraMap<EventRuleId, EventRule>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for EventRuleTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for EventRuleTable {
    type Key = EventRuleId;
    type Row = EventRule;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
