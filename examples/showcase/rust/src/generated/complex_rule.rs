// This file was generated by Sora. Do not edit manually.
use super::SoraMap;

#[repr(transparent)]
#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, serde::Serialize, serde::Deserialize,
)]
#[serde(transparent)]
pub struct ComplexRuleId(i32);

impl ComplexRuleId {
    pub const fn from_raw(raw: i32) -> Self {
        Self(raw)
    }

    pub const fn raw(self) -> i32 {
        self.0
    }
}

impl From<i32> for ComplexRuleId {
    fn from(raw: i32) -> Self {
        Self::from_raw(raw)
    }
}

impl super::runtime::SoraDecode for ComplexRuleId {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self::from_raw(<i32 as super::runtime::SoraDecode>::decode(
            reader,
        )?))
    }
}

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct ComplexRule {
    pub id: ComplexRuleId,
    pub name: std::sync::Arc<str>,
    /// Single union value derived from a tagged_columns child row
    pub root_condition: super::event_condition::EventCondition,
    pub root_action_group: super::complex_action_group::ComplexActionGroupId,
    /// Non-JSON list<union<RewardAction>> assembled from child rows
    pub actions: Vec<super::reward_action::RewardAction>,
    /// Nested tuple, tuple_list, split, and map parsers in one cell
    pub budget: super::complex_budget::ComplexBudget,
}

impl super::runtime::SoraDecode for ComplexRule {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            id: <ComplexRuleId as super::runtime::SoraDecode>::decode(reader)?,
            name: <std::sync::Arc<str> as super::runtime::SoraDecode>::decode(reader)?,
            root_condition: <super::event_condition::EventCondition as super::runtime::SoraDecode>::decode(reader)?,
            root_action_group: <super::complex_action_group::ComplexActionGroupId as super::runtime::SoraDecode>::decode(reader)?,
            actions: { let len = reader.read_var_u32()? as usize; let mut values = Vec::with_capacity(len); for _ in 0..len { values.push(<super::reward_action::RewardAction as super::runtime::SoraDecode>::decode(reader)?); } values },
            budget: <super::complex_budget::ComplexBudget as super::runtime::SoraDecode>::decode(reader)?,
        })
    }
}

impl ComplexRule {
    pub(crate) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
        self.root_condition.collect_text_keys(out);
        for value in self.actions.iter() {
            value.collect_text_keys(out);
        }
        self.budget.collect_text_keys(out);
    }
}

#[derive(Debug, Clone)]
pub struct ComplexRuleTable {
    keys: Vec<ComplexRuleId>,
    rows: SoraMap<ComplexRuleId, ComplexRule>,
}

impl ComplexRuleTable {
    pub const NAME: &'static str = "ComplexRule";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "ComplexRule",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "id",
            ty: "ComplexRuleId",
        }),
        indexes: &[],
    };

    pub(crate) fn from_rows(rows: Vec<ComplexRule>) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.id).collect::<Vec<_>>();
        Ok(Self {
            keys,
            rows: super::decode_map_table(rows, |row| row.id),
        })
    }

    pub fn get(&self, key: &ComplexRuleId) -> Option<&ComplexRule> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[ComplexRuleId] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &ComplexRule> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for ComplexRuleTable {
    type Target = SoraMap<ComplexRuleId, ComplexRule>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for ComplexRuleTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for ComplexRuleTable {
    type Key = ComplexRuleId;
    type Row = ComplexRule;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
