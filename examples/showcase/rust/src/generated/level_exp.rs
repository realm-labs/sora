// This file was generated by Sora. Do not edit manually.
use super::SoraMap;

#[repr(transparent)]
#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, serde::Serialize, serde::Deserialize,
)]
#[serde(transparent)]
pub struct LevelExpLevel(i32);

impl LevelExpLevel {
    pub const fn from_raw(raw: i32) -> Self {
        Self(raw)
    }

    pub const fn raw(self) -> i32 {
        self.0
    }
}

impl From<i32> for LevelExpLevel {
    fn from(raw: i32) -> Self {
        Self::from_raw(raw)
    }
}

impl super::runtime::SoraDecode for LevelExpLevel {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self::from_raw(<i32 as super::runtime::SoraDecode>::decode(
            reader,
        )?))
    }
}

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct LevelExp {
    pub level: LevelExpLevel,
    pub exp: i64,
    pub unlock_feature: Option<std::sync::Arc<str>>,
}

impl super::runtime::SoraDecode for LevelExp {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            level: <LevelExpLevel as super::runtime::SoraDecode>::decode(reader)?,
            exp: <i64 as super::runtime::SoraDecode>::decode(reader)?,
            unlock_feature: match reader.read_u8()? {
                0 => None,
                1 => Some(<std::sync::Arc<str> as super::runtime::SoraDecode>::decode(
                    reader,
                )?),
                value => {
                    return Err(super::runtime::SoraReadError::new(format!(
                        "invalid option presence {}",
                        value
                    )));
                }
            },
        })
    }
}

impl LevelExp {
    pub(crate) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
    }
}

#[derive(Debug, Clone)]
pub struct LevelExpTable {
    keys: Vec<LevelExpLevel>,
    rows: SoraMap<LevelExpLevel, LevelExp>,
}

impl LevelExpTable {
    pub const NAME: &'static str = "LevelExp";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "LevelExp",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "level",
            ty: "LevelExpLevel",
        }),
        indexes: &[],
    };

    pub(crate) fn from_rows(rows: Vec<LevelExp>) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.level).collect::<Vec<_>>();
        Ok(Self {
            keys,
            rows: super::decode_map_table(rows, |row| row.level),
        })
    }

    pub fn get(&self, key: &LevelExpLevel) -> Option<&LevelExp> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[LevelExpLevel] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &LevelExp> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for LevelExpTable {
    type Target = SoraMap<LevelExpLevel, LevelExp>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for LevelExpTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for LevelExpTable {
    type Key = LevelExpLevel;
    type Row = LevelExp;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
