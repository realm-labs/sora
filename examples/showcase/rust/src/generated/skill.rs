// This file was generated by Sora. Do not edit manually.
use super::SoraMap;

#[repr(transparent)]
#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, serde::Serialize, serde::Deserialize,
)]
#[serde(transparent)]
pub struct SkillId(i32);

impl SkillId {
    pub const fn from_raw(raw: i32) -> Self {
        Self(raw)
    }

    pub const fn raw(self) -> i32 {
        self.0
    }
}

impl From<i32> for SkillId {
    fn from(raw: i32) -> Self {
        Self::from_raw(raw)
    }
}

impl super::runtime::SoraDecode for SkillId {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self::from_raw(<i32 as super::runtime::SoraDecode>::decode(
            reader,
        )?))
    }
}

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct Skill {
    pub id: SkillId,
    pub name: std::sync::Arc<str>,
    pub element: super::element_type::ElementType,
    /// Tuple cost, e.g. Gold,0,150
    pub cost: super::resource_cost::ResourceCost,
    /// JSON object with element/power/radius
    pub effect: super::skill_effect::SkillEffect,
    pub required_level: i32,
    /// Optional item requirement
    pub required_item: Option<super::item::ItemId>,
    pub cast_origin: super::vec3::Vec3,
}

impl super::runtime::SoraDecode for Skill {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            id: <SkillId as super::runtime::SoraDecode>::decode(reader)?,
            name: <std::sync::Arc<str> as super::runtime::SoraDecode>::decode(reader)?,
            element: <super::element_type::ElementType as super::runtime::SoraDecode>::decode(
                reader,
            )?,
            cost: <super::resource_cost::ResourceCost as super::runtime::SoraDecode>::decode(
                reader,
            )?,
            effect: <super::skill_effect::SkillEffect as super::runtime::SoraDecode>::decode(
                reader,
            )?,
            required_level: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            required_item: match reader.read_u8()? {
                0 => None,
                1 => Some(<super::item::ItemId as super::runtime::SoraDecode>::decode(
                    reader,
                )?),
                value => {
                    return Err(super::runtime::SoraReadError::new(format!(
                        "invalid option presence {}",
                        value
                    )));
                }
            },
            cast_origin: <super::vec3::Vec3 as super::runtime::SoraDecode>::decode(reader)?,
        })
    }
}

impl Skill {
    pub(crate) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
        self.cost.collect_text_keys(out);
        self.effect.collect_text_keys(out);
        self.cast_origin.collect_text_keys(out);
    }
}

#[derive(Debug, Clone)]
pub struct SkillTable {
    keys: Vec<SkillId>,
    rows: SoraMap<SkillId, Skill>,
}

impl SkillTable {
    pub const NAME: &'static str = "Skill";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "Skill",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "id",
            ty: "SkillId",
        }),
        indexes: &[],
    };

    pub(crate) fn from_rows(rows: Vec<Skill>) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.id).collect::<Vec<_>>();
        Ok(Self {
            keys,
            rows: super::decode_map_table(rows, |row| row.id),
        })
    }

    pub fn get(&self, key: &SkillId) -> Option<&Skill> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[SkillId] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &Skill> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for SkillTable {
    type Target = SoraMap<SkillId, Skill>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for SkillTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for SkillTable {
    type Key = SkillId;
    type Row = Skill;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
