// This file was generated by Sora. Do not edit manually.
use super::SoraMap;

#[repr(transparent)]
#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, serde::Serialize, serde::Deserialize,
)]
#[serde(transparent)]
pub struct StageId(i32);

impl StageId {
    pub const fn from_raw(raw: i32) -> Self {
        Self(raw)
    }

    pub const fn raw(self) -> i32 {
        self.0
    }
}

impl From<i32> for StageId {
    fn from(raw: i32) -> Self {
        Self::from_raw(raw)
    }
}

impl super::runtime::SoraDecode for StageId {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self::from_raw(<i32 as super::runtime::SoraDecode>::decode(
            reader,
        )?))
    }
}

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct Stage {
    pub id: StageId,
    pub name: std::sync::Arc<str>,
    pub monster_ids: Vec<super::monster::MonsterId>,
    pub recommended_power: i32,
    pub first_clear_rewards: Vec<super::reward::Reward>,
}

impl super::runtime::SoraDecode for Stage {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            id: <StageId as super::runtime::SoraDecode>::decode(reader)?,
            name: <std::sync::Arc<str> as super::runtime::SoraDecode>::decode(reader)?,
            monster_ids: {
                let len = reader.read_var_u32()? as usize;
                let mut values = Vec::with_capacity(len);
                for _ in 0..len {
                    values.push(
                        <super::monster::MonsterId as super::runtime::SoraDecode>::decode(reader)?,
                    );
                }
                values
            },
            recommended_power: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            first_clear_rewards: {
                let len = reader.read_var_u32()? as usize;
                let mut values = Vec::with_capacity(len);
                for _ in 0..len {
                    values.push(
                        <super::reward::Reward as super::runtime::SoraDecode>::decode(reader)?,
                    );
                }
                values
            },
        })
    }
}

impl Stage {
    pub(crate) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
        for value in self.first_clear_rewards.iter() {
            value.collect_text_keys(out);
        }
    }
}

#[derive(Debug, Clone)]
pub struct StageTable {
    keys: Vec<StageId>,
    rows: SoraMap<StageId, Stage>,
}

impl StageTable {
    pub const NAME: &'static str = "Stage";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "Stage",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "id",
            ty: "StageId",
        }),
        indexes: &[],
    };

    pub(crate) fn from_rows(rows: Vec<Stage>) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.id).collect::<Vec<_>>();
        Ok(Self {
            keys,
            rows: super::decode_map_table(rows, |row| row.id),
        })
    }

    pub fn get(&self, key: &StageId) -> Option<&Stage> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[StageId] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &Stage> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for StageTable {
    type Target = SoraMap<StageId, Stage>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for StageTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for StageTable {
    type Key = StageId;
    type Row = Stage;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
