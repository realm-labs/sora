// This file was generated by Sora. Do not edit manually.
use super::SoraMap;

#[repr(transparent)]
#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, serde::Serialize, serde::Deserialize,
)]
#[serde(transparent)]
pub struct QuestId(i32);

impl QuestId {
    pub const fn from_raw(raw: i32) -> Self {
        Self(raw)
    }

    pub const fn raw(self) -> i32 {
        self.0
    }
}

impl From<i32> for QuestId {
    fn from(raw: i32) -> Self {
        Self::from_raw(raw)
    }
}

impl super::runtime::SoraDecode for QuestId {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self::from_raw(<i32 as super::runtime::SoraDecode>::decode(
            reader,
        )?))
    }
}

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct Quest {
    pub id: QuestId,
    pub quest_type: super::quest_type::QuestType,
    pub title: std::sync::Arc<str>,
    pub required_item: super::item::ItemId,
    pub unlock_skills: Vec<super::skill::SkillId>,
    pub start_pos: super::vec3::Vec3,
    /// Materialized from QuestReward child rows
    pub rewards: Vec<super::reward::Reward>,
}

impl super::runtime::SoraDecode for Quest {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            id: <QuestId as super::runtime::SoraDecode>::decode(reader)?,
            quest_type: <super::quest_type::QuestType as super::runtime::SoraDecode>::decode(
                reader,
            )?,
            title: <std::sync::Arc<str> as super::runtime::SoraDecode>::decode(reader)?,
            required_item: <super::item::ItemId as super::runtime::SoraDecode>::decode(reader)?,
            unlock_skills: {
                let len = reader.read_var_u32()? as usize;
                let mut values = Vec::with_capacity(len);
                for _ in 0..len {
                    values.push(
                        <super::skill::SkillId as super::runtime::SoraDecode>::decode(reader)?,
                    );
                }
                values
            },
            start_pos: <super::vec3::Vec3 as super::runtime::SoraDecode>::decode(reader)?,
            rewards: {
                let len = reader.read_var_u32()? as usize;
                let mut values = Vec::with_capacity(len);
                for _ in 0..len {
                    values.push(
                        <super::reward::Reward as super::runtime::SoraDecode>::decode(reader)?,
                    );
                }
                values
            },
        })
    }
}

impl Quest {
    pub(crate) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
        self.start_pos.collect_text_keys(out);
        for value in self.rewards.iter() {
            value.collect_text_keys(out);
        }
    }
}

#[derive(Debug, Clone)]
pub struct QuestTable {
    keys: Vec<QuestId>,
    rows: SoraMap<QuestId, Quest>,
}

impl QuestTable {
    pub const NAME: &'static str = "Quest";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "Quest",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "id",
            ty: "QuestId",
        }),
        indexes: &[],
    };

    pub(crate) fn from_rows(rows: Vec<Quest>) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.id).collect::<Vec<_>>();
        Ok(Self {
            keys,
            rows: super::decode_map_table(rows, |row| row.id),
        })
    }

    pub fn get(&self, key: &QuestId) -> Option<&Quest> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[QuestId] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &Quest> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for QuestTable {
    type Target = SoraMap<QuestId, Quest>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for QuestTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for QuestTable {
    type Key = QuestId;
    type Row = Quest;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
