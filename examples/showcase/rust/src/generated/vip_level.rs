// This file was generated by Sora. Do not edit manually.
use super::SoraMap;

#[repr(transparent)]
#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, serde::Serialize, serde::Deserialize,
)]
#[serde(transparent)]
pub struct VipLevelLevel(i32);

impl VipLevelLevel {
    pub const fn from_raw(raw: i32) -> Self {
        Self(raw)
    }

    pub const fn raw(self) -> i32 {
        self.0
    }
}

impl From<i32> for VipLevelLevel {
    fn from(raw: i32) -> Self {
        Self::from_raw(raw)
    }
}

impl super::runtime::SoraDecode for VipLevelLevel {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self::from_raw(<i32 as super::runtime::SoraDecode>::decode(
            reader,
        )?))
    }
}

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct VipLevel {
    pub level: VipLevelLevel,
    pub cost: super::resource_cost::ResourceCost,
    pub perks: Vec<std::sync::Arc<str>>,
}

impl super::runtime::SoraDecode for VipLevel {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            level: <VipLevelLevel as super::runtime::SoraDecode>::decode(reader)?,
            cost: <super::resource_cost::ResourceCost as super::runtime::SoraDecode>::decode(
                reader,
            )?,
            perks: {
                let len = reader.read_var_u32()? as usize;
                let mut values = Vec::with_capacity(len);
                for _ in 0..len {
                    values.push(<std::sync::Arc<str> as super::runtime::SoraDecode>::decode(
                        reader,
                    )?);
                }
                values
            },
        })
    }
}

impl VipLevel {
    pub(crate) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
        self.cost.collect_text_keys(out);
    }
}

#[derive(Debug, Clone)]
pub struct VipLevelTable {
    keys: Vec<VipLevelLevel>,
    rows: SoraMap<VipLevelLevel, VipLevel>,
}

impl VipLevelTable {
    pub const NAME: &'static str = "VipLevel";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "VipLevel",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "level",
            ty: "VipLevelLevel",
        }),
        indexes: &[],
    };

    pub(crate) fn from_rows(rows: Vec<VipLevel>) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.level).collect::<Vec<_>>();
        Ok(Self {
            keys,
            rows: super::decode_map_table(rows, |row| row.level),
        })
    }

    pub fn get(&self, key: &VipLevelLevel) -> Option<&VipLevel> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[VipLevelLevel] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &VipLevel> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for VipLevelTable {
    type Target = SoraMap<VipLevelLevel, VipLevel>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for VipLevelTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for VipLevelTable {
    type Key = VipLevelLevel;
    type Row = VipLevel;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
