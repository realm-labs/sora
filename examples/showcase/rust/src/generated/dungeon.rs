// This file was generated by Sora. Do not edit manually.
use super::SoraMap;

#[repr(transparent)]
#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, serde::Serialize, serde::Deserialize,
)]
#[serde(transparent)]
pub struct DungeonId(i32);

impl DungeonId {
    pub const fn from_raw(raw: i32) -> Self {
        Self(raw)
    }

    pub const fn raw(self) -> i32 {
        self.0
    }
}

impl From<i32> for DungeonId {
    fn from(raw: i32) -> Self {
        Self::from_raw(raw)
    }
}

impl super::runtime::SoraDecode for DungeonId {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self::from_raw(<i32 as super::runtime::SoraDecode>::decode(
            reader,
        )?))
    }
}

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct Dungeon {
    pub id: DungeonId,
    pub name: std::sync::Arc<str>,
    pub stage_ids: Vec<super::stage::StageId>,
    pub entry_cost: super::resource_cost::ResourceCost,
}

impl super::runtime::SoraDecode for Dungeon {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            id: <DungeonId as super::runtime::SoraDecode>::decode(reader)?,
            name: <std::sync::Arc<str> as super::runtime::SoraDecode>::decode(reader)?,
            stage_ids: {
                let len = reader.read_var_u32()? as usize;
                let mut values = Vec::with_capacity(len);
                for _ in 0..len {
                    values.push(
                        <super::stage::StageId as super::runtime::SoraDecode>::decode(reader)?,
                    );
                }
                values
            },
            entry_cost: <super::resource_cost::ResourceCost as super::runtime::SoraDecode>::decode(
                reader,
            )?,
        })
    }
}

impl Dungeon {
    pub(crate) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
        self.entry_cost.collect_text_keys(out);
    }
}

#[derive(Debug, Clone)]
pub struct DungeonTable {
    keys: Vec<DungeonId>,
    rows: SoraMap<DungeonId, Dungeon>,
}

impl DungeonTable {
    pub const NAME: &'static str = "Dungeon";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "Dungeon",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "id",
            ty: "DungeonId",
        }),
        indexes: &[],
    };

    pub(crate) fn from_rows(rows: Vec<Dungeon>) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.id).collect::<Vec<_>>();
        Ok(Self {
            keys,
            rows: super::decode_map_table(rows, |row| row.id),
        })
    }

    pub fn get(&self, key: &DungeonId) -> Option<&Dungeon> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[DungeonId] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &Dungeon> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for DungeonTable {
    type Target = SoraMap<DungeonId, Dungeon>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for DungeonTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for DungeonTable {
    type Key = DungeonId;
    type Row = Dungeon;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
