// This file was generated by Sora. Do not edit manually.
use super::SoraMap;

#[repr(transparent)]
#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, serde::Serialize, serde::Deserialize,
)]
#[serde(transparent)]
pub struct DialogueId(i32);

impl DialogueId {
    pub const fn from_raw(raw: i32) -> Self {
        Self(raw)
    }

    pub const fn raw(self) -> i32 {
        self.0
    }
}

impl From<i32> for DialogueId {
    fn from(raw: i32) -> Self {
        Self::from_raw(raw)
    }
}

impl super::runtime::SoraDecode for DialogueId {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self::from_raw(<i32 as super::runtime::SoraDecode>::decode(
            reader,
        )?))
    }
}

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct Dialogue {
    pub id: DialogueId,
    pub speaker_key: super::runtime::TextKey,
    pub lines: Vec<std::sync::Arc<str>>,
}

impl super::runtime::SoraDecode for Dialogue {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            id: <DialogueId as super::runtime::SoraDecode>::decode(reader)?,
            speaker_key: <super::runtime::TextKey as super::runtime::SoraDecode>::decode(reader)?,
            lines: {
                let len = reader.read_var_u32()? as usize;
                let mut values = Vec::with_capacity(len);
                for _ in 0..len {
                    values.push(<std::sync::Arc<str> as super::runtime::SoraDecode>::decode(
                        reader,
                    )?);
                }
                values
            },
        })
    }
}

impl Dialogue {
    pub(crate) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
        out.push(&self.speaker_key);
    }
}

#[derive(Debug, Clone)]
pub struct DialogueTable {
    keys: Vec<DialogueId>,
    rows: SoraMap<DialogueId, Dialogue>,
}

impl DialogueTable {
    pub const NAME: &'static str = "Dialogue";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "Dialogue",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "id",
            ty: "DialogueId",
        }),
        indexes: &[],
    };

    pub(crate) fn from_rows(rows: Vec<Dialogue>) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.id).collect::<Vec<_>>();
        Ok(Self {
            keys,
            rows: super::decode_map_table(rows, |row| row.id),
        })
    }

    pub fn get(&self, key: &DialogueId) -> Option<&Dialogue> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[DialogueId] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &Dialogue> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for DialogueTable {
    type Target = SoraMap<DialogueId, Dialogue>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for DialogueTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for DialogueTable {
    type Key = DialogueId;
    type Row = Dialogue;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
