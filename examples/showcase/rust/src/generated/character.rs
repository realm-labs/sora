// This file was generated by Sora. Do not edit manually.
use super::SoraMap;

#[repr(transparent)]
#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, serde::Serialize, serde::Deserialize,
)]
#[serde(transparent)]
pub struct CharacterId(i32);

impl CharacterId {
    pub const fn from_raw(raw: i32) -> Self {
        Self(raw)
    }

    pub const fn raw(self) -> i32 {
        self.0
    }
}

impl From<i32> for CharacterId {
    fn from(raw: i32) -> Self {
        Self::from_raw(raw)
    }
}

impl super::runtime::SoraDecode for CharacterId {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self::from_raw(<i32 as super::runtime::SoraDecode>::decode(
            reader,
        )?))
    }
}

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct Character {
    pub id: CharacterId,
    pub name: std::sync::Arc<str>,
    pub rarity: super::rarity::Rarity,
    pub base_level: super::level_exp::LevelExpLevel,
    pub base_skill: super::skill::SkillId,
    pub starter_items: Vec<super::item::ItemId>,
    pub spawn_pos: super::vec3::Vec3,
}

impl super::runtime::SoraDecode for Character {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            id: <CharacterId as super::runtime::SoraDecode>::decode(reader)?,
            name: <std::sync::Arc<str> as super::runtime::SoraDecode>::decode(reader)?,
            rarity: <super::rarity::Rarity as super::runtime::SoraDecode>::decode(reader)?,
            base_level: <super::level_exp::LevelExpLevel as super::runtime::SoraDecode>::decode(
                reader,
            )?,
            base_skill: <super::skill::SkillId as super::runtime::SoraDecode>::decode(reader)?,
            starter_items: {
                let len = reader.read_var_u32()? as usize;
                let mut values = Vec::with_capacity(len);
                for _ in 0..len {
                    values.push(<super::item::ItemId as super::runtime::SoraDecode>::decode(
                        reader,
                    )?);
                }
                values
            },
            spawn_pos: <super::vec3::Vec3 as super::runtime::SoraDecode>::decode(reader)?,
        })
    }
}

impl Character {
    pub(crate) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
        self.spawn_pos.collect_text_keys(out);
    }
}

#[derive(Debug, Clone)]
pub struct CharacterTable {
    keys: Vec<CharacterId>,
    rows: SoraMap<CharacterId, Character>,
}

impl CharacterTable {
    pub const NAME: &'static str = "Character";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "Character",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "id",
            ty: "CharacterId",
        }),
        indexes: &[],
    };

    pub(crate) fn from_rows(rows: Vec<Character>) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.id).collect::<Vec<_>>();
        Ok(Self {
            keys,
            rows: super::decode_map_table(rows, |row| row.id),
        })
    }

    pub fn get(&self, key: &CharacterId) -> Option<&Character> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[CharacterId] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &Character> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for CharacterTable {
    type Target = SoraMap<CharacterId, Character>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for CharacterTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for CharacterTable {
    type Key = CharacterId;
    type Row = Character;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
