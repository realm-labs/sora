// This file was generated by Sora. Do not edit manually.

import type { SoraKeyedTable, SoraTableInfo, SoraReader, SoraValue, TextKey } from "./sora_runtime.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";

import type { ResourceCost } from "./resource_cost.js";
import { collectResourceCostTextKeys, decodeResourceCost, decodeResourceCostValue } from "./resource_cost.js";


export interface Dungeon {
    readonly id: number;
    readonly name: string;
    readonly stageIds: readonly number[];
    readonly entryCost: ResourceCost;
}

export function decodeDungeon(reader: SoraReader): Dungeon {
    return {
        id: reader.readI32(),
        name: reader.readString(),
        stageIds: reader.readList(() => reader.readI32()),
        entryCost: decodeResourceCost(reader),
    };
}

export function decodeDungeonValue(value: SoraValue): Dungeon {
    const object = value.asObject();
    return {
        id: object.get("id").asInt(),
        name: object.get("name").asString(),
        stageIds: object.get("stage_ids").asList((item) => item.asInt()),
        entryCost: decodeResourceCostValue(object.get("entry_cost")),
    };
}

export function collectDungeonTextKeys(value: Dungeon, out: TextKey[]): void {
    collectResourceCostTextKeys(value.entryCost, out);
}

export class DungeonTable implements SoraKeyedTable<number, Dungeon> {
    static readonly tableName = "Dungeon";
    static readonly tableInfo: SoraTableInfo = {
        name: DungeonTable.tableName,
        rowType: "Dungeon",
        shape: "keyed",
        primaryKey: { name: "id", type: "number" },
        indexes: [
        ],
    };

    private constructor(
        private readonly _keys: readonly number[],
        private readonly _rows: ReadonlyMap<number, Dungeon>,
    ) {}

    static decode(rows: Dungeon[]): DungeonTable {
        return new DungeonTable(
            rows.map((row) => row.id),
            decodeMapTable(rows, (row) => row.id),
        );
    }

    info(): SoraTableInfo {
        return DungeonTable.tableInfo;
    }

    get size(): number {
        return this._rows.size;
    }
    get(key: number): Dungeon | undefined {
        return this._rows.get(key);
    }

    has(key: number): boolean {
        return this._rows.has(key);
    }

    get rows(): ReadonlyMap<number, Dungeon> {
        return this._rows;
    }

    get orderedKeys(): readonly number[] {
        return this._keys;
    }

    get orderedRows(): readonly Dungeon[] {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }

    [Symbol.iterator](): Iterator<Dungeon> {
        return this._rows.values();
    }
}
