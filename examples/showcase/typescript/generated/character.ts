// This file was generated by Sora. Do not edit manually.

import type { SoraConfigTable, SoraReader, SoraValue } from "./sora_runtime.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";

import type { Rarity } from "./rarity.js";
import { decodeRarity, decodeRarityValue } from "./rarity.js";

import type { Vec3 } from "./vec3.js";
import { decodeVec3, decodeVec3Value } from "./vec3.js";


export interface Character {
    id: number;
    name: string;
    rarity: Rarity;
    baseLevel: number;
    baseSkill: number;
    starterItems: number[];
    spawnPos: Vec3;
}

export function decodeCharacter(reader: SoraReader): Character {
    return {
        id: reader.readI32(),
        name: reader.readString(),
        rarity: decodeRarity(reader),
        baseLevel: reader.readI32(),
        baseSkill: reader.readI32(),
        starterItems: reader.readList(() => reader.readI32()),
        spawnPos: decodeVec3(reader),
    };
}

export function decodeCharacterValue(value: SoraValue): Character {
    const object = value.asObject();
    return {
        id: object.get("id").asInt(),
        name: object.get("name").asString(),
        rarity: decodeRarityValue(object.get("rarity")),
        baseLevel: object.get("base_level").asInt(),
        baseSkill: object.get("base_skill").asInt(),
        starterItems: object.get("starter_items").asList((item) => item.asInt()),
        spawnPos: decodeVec3Value(object.get("spawn_pos")),
    };
}

export class CharacterTable implements SoraConfigTable {
    static readonly tableName = "Character";

    private constructor(
        private readonly _keys: number[],
        private readonly _rows: Map<number, Character>,
    ) {}

    static decode(rows: Character[]): CharacterTable {
        return new CharacterTable(
            rows.map((row) => row.id),
            decodeMapTable(rows, (row) => row.id),
        );
    }

    name(): string {
        return CharacterTable.tableName;
    }

    mode(): string {
        return "map";
    }

    key(): string | undefined {
        return "id";
    }

    len(): number {
        return this._rows.size;
    }
    get(key: number): Character | undefined {
        return this._rows.get(key);
    }

    rows(): ReadonlyMap<number, Character> {
        return this._rows;
    }

    keys(): readonly number[] {
        return this._keys;
    }

    orderedRows(): Character[] {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }
}
