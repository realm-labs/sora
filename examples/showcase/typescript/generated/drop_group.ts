// This file was generated by Sora. Do not edit manually.

import type { SoraKeyedTable, SoraTableInfo, SoraReader, SoraValue, TextKey } from "./sora_runtime.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";


export interface DropGroup {
    readonly id: number;
    readonly name: string;
}

export function decodeDropGroup(reader: SoraReader): DropGroup {
    return {
        id: reader.readI32(),
        name: reader.readString(),
    };
}

export function decodeDropGroupValue(value: SoraValue): DropGroup {
    const object = value.asObject();
    return {
        id: object.get("id").asInt(),
        name: object.get("name").asString(),
    };
}

export function collectDropGroupTextKeys(value: DropGroup, out: TextKey[]): void {
}

export class DropGroupTable implements SoraKeyedTable<number, DropGroup> {
    static readonly tableName = "DropGroup";
    static readonly tableInfo: SoraTableInfo = {
        name: DropGroupTable.tableName,
        rowType: "DropGroup",
        shape: "keyed",
        primaryKey: { name: "id", type: "number" },
        indexes: [
        ],
    };

    private constructor(
        private readonly _keys: readonly number[],
        private readonly _rows: ReadonlyMap<number, DropGroup>,
    ) {}

    static decode(rows: DropGroup[]): DropGroupTable {
        return new DropGroupTable(
            rows.map((row) => row.id),
            decodeMapTable(rows, (row) => row.id),
        );
    }

    info(): SoraTableInfo {
        return DropGroupTable.tableInfo;
    }

    get size(): number {
        return this._rows.size;
    }
    get(key: number): DropGroup | undefined {
        return this._rows.get(key);
    }

    has(key: number): boolean {
        return this._rows.has(key);
    }

    get rows(): ReadonlyMap<number, DropGroup> {
        return this._rows;
    }

    get orderedKeys(): readonly number[] {
        return this._keys;
    }

    get orderedRows(): readonly DropGroup[] {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }

    [Symbol.iterator](): Iterator<DropGroup> {
        return this._rows.values();
    }
}
