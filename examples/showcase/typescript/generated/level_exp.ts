// This file was generated by Sora. Do not edit manually.

import type { SoraKeyedTable, SoraTableInfo, SoraReader, SoraValue, TextKey } from "./sora_runtime.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";


export interface LevelExp {
    readonly level: number;
    readonly exp: bigint;
    readonly unlockFeature: string | undefined;
}

export function decodeLevelExp(reader: SoraReader): LevelExp {
    return {
        level: reader.readI32(),
        exp: reader.readI64(),
        unlockFeature: reader.readOptional(() => reader.readString()),
    };
}

export function decodeLevelExpValue(value: SoraValue): LevelExp {
    const object = value.asObject();
    return {
        level: object.get("level").asInt(),
        exp: object.get("exp").asBigInt(),
        unlockFeature: object.get("unlock_feature").isNull() ? undefined : object.get("unlock_feature").asString(),
    };
}

export function collectLevelExpTextKeys(value: LevelExp, out: TextKey[]): void {
}

export class LevelExpTable implements SoraKeyedTable<number, LevelExp> {
    static readonly tableName = "LevelExp";
    static readonly tableInfo: SoraTableInfo = {
        name: LevelExpTable.tableName,
        rowType: "LevelExp",
        shape: "keyed",
        primaryKey: { name: "level", type: "number" },
        indexes: [
        ],
    };

    private constructor(
        private readonly _keys: readonly number[],
        private readonly _rows: ReadonlyMap<number, LevelExp>,
    ) {}

    static decode(rows: LevelExp[]): LevelExpTable {
        return new LevelExpTable(
            rows.map((row) => row.level),
            decodeMapTable(rows, (row) => row.level),
        );
    }

    info(): SoraTableInfo {
        return LevelExpTable.tableInfo;
    }

    get size(): number {
        return this._rows.size;
    }
    get(key: number): LevelExp | undefined {
        return this._rows.get(key);
    }

    has(key: number): boolean {
        return this._rows.has(key);
    }

    get rows(): ReadonlyMap<number, LevelExp> {
        return this._rows;
    }

    get orderedKeys(): readonly number[] {
        return this._keys;
    }

    get orderedRows(): readonly LevelExp[] {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }

    [Symbol.iterator](): Iterator<LevelExp> {
        return this._rows.values();
    }
}
