// This file was generated by Sora. Do not edit manually.

import type { SoraKeyedTable, SoraTableInfo, SoraReader, SoraValue, TextKey } from "./sora_runtime.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";

import type { Reward } from "./reward.js";
import { collectRewardTextKeys, decodeReward, decodeRewardValue } from "./reward.js";


export interface Stage {
    readonly id: number;
    readonly name: string;
    readonly monsterIds: readonly number[];
    readonly recommendedPower: number;
    readonly firstClearRewards: readonly Reward[];
}

export function decodeStage(reader: SoraReader): Stage {
    return {
        id: reader.readI32(),
        name: reader.readString(),
        monsterIds: reader.readList(() => reader.readI32()),
        recommendedPower: reader.readI32(),
        firstClearRewards: reader.readList(() => decodeReward(reader)),
    };
}

export function decodeStageValue(value: SoraValue): Stage {
    const object = value.asObject();
    return {
        id: object.get("id").asInt(),
        name: object.get("name").asString(),
        monsterIds: object.get("monster_ids").asList((item) => item.asInt()),
        recommendedPower: object.get("recommended_power").asInt(),
        firstClearRewards: object.get("first_clear_rewards").asList((item) => decodeRewardValue(item)),
    };
}

export function collectStageTextKeys(value: Stage, out: TextKey[]): void {
    for (const item of value.firstClearRewards) { collectRewardTextKeys(item, out); }
}

export class StageTable implements SoraKeyedTable<number, Stage> {
    static readonly tableName = "Stage";
    static readonly tableInfo: SoraTableInfo = {
        name: StageTable.tableName,
        rowType: "Stage",
        shape: "keyed",
        primaryKey: { name: "id", type: "number" },
        indexes: [
        ],
    };

    private constructor(
        private readonly _keys: readonly number[],
        private readonly _rows: ReadonlyMap<number, Stage>,
    ) {}

    static decode(rows: Stage[]): StageTable {
        return new StageTable(
            rows.map((row) => row.id),
            decodeMapTable(rows, (row) => row.id),
        );
    }

    info(): SoraTableInfo {
        return StageTable.tableInfo;
    }

    get size(): number {
        return this._rows.size;
    }
    get(key: number): Stage | undefined {
        return this._rows.get(key);
    }

    has(key: number): boolean {
        return this._rows.has(key);
    }

    get rows(): ReadonlyMap<number, Stage> {
        return this._rows;
    }

    get orderedKeys(): readonly number[] {
        return this._keys;
    }

    get orderedRows(): readonly Stage[] {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }

    [Symbol.iterator](): Iterator<Stage> {
        return this._rows.values();
    }
}
