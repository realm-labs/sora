// This file was generated by Sora. Do not edit manually.

import type { SoraKeyedTable, SoraTableInfo, SoraReader, SoraValue } from "./sora_runtime.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";

import type { ElementType } from "./element_type.js";
import { decodeElementType, decodeElementTypeValue } from "./element_type.js";

import type { Vec3 } from "./vec3.js";
import { decodeVec3, decodeVec3Value } from "./vec3.js";


export interface Monster {
    id: number;
    name: string;
    level: number;
    element: ElementType;
    dropGroup: number;
    spawnPos: Vec3;
}

export function decodeMonster(reader: SoraReader): Monster {
    return {
        id: reader.readI32(),
        name: reader.readString(),
        level: reader.readI32(),
        element: decodeElementType(reader),
        dropGroup: reader.readI32(),
        spawnPos: decodeVec3(reader),
    };
}

export function decodeMonsterValue(value: SoraValue): Monster {
    const object = value.asObject();
    return {
        id: object.get("id").asInt(),
        name: object.get("name").asString(),
        level: object.get("level").asInt(),
        element: decodeElementTypeValue(object.get("element")),
        dropGroup: object.get("drop_group").asInt(),
        spawnPos: decodeVec3Value(object.get("spawn_pos")),
    };
}

export class MonsterTable implements SoraKeyedTable<number, Monster> {
    static readonly tableName = "Monster";
    static readonly tableInfo: SoraTableInfo = {
        name: MonsterTable.tableName,
        rowType: "Monster",
        shape: "keyed",
        primaryKey: { name: "id", type: "number" },
        indexes: [
        ],
    };

    private constructor(
        private readonly _keys: number[],
        private readonly _rows: Map<number, Monster>,
    ) {}

    static decode(rows: Monster[]): MonsterTable {
        return new MonsterTable(
            rows.map((row) => row.id),
            decodeMapTable(rows, (row) => row.id),
        );
    }

    info(): SoraTableInfo {
        return MonsterTable.tableInfo;
    }

    len(): number {
        return this._rows.size;
    }
    get(key: number): Monster | undefined {
        return this._rows.get(key);
    }

    rows(): ReadonlyMap<number, Monster> {
        return this._rows;
    }

    keys(): readonly number[] {
        return this._keys;
    }

    orderedRows(): Monster[] {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }
}
