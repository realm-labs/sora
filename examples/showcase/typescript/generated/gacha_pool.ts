// This file was generated by Sora. Do not edit manually.

import type { SoraKeyedTable, SoraTableInfo, SoraReader, SoraValue, TextKey } from "./sora_runtime.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";

import type { ResourceCost } from "./resource_cost.js";
import { collectResourceCostTextKeys, decodeResourceCost, decodeResourceCostValue } from "./resource_cost.js";


export interface GachaPool {
    id: number;
    name: string;
    cost: ResourceCost;
}

export function decodeGachaPool(reader: SoraReader): GachaPool {
    return {
        id: reader.readI32(),
        name: reader.readString(),
        cost: decodeResourceCost(reader),
    };
}

export function decodeGachaPoolValue(value: SoraValue): GachaPool {
    const object = value.asObject();
    return {
        id: object.get("id").asInt(),
        name: object.get("name").asString(),
        cost: decodeResourceCostValue(object.get("cost")),
    };
}

export function collectGachaPoolTextKeys(value: GachaPool, out: TextKey[]): void {
    collectResourceCostTextKeys(value.cost, out);
}

export class GachaPoolTable implements SoraKeyedTable<number, GachaPool> {
    static readonly tableName = "GachaPool";
    static readonly tableInfo: SoraTableInfo = {
        name: GachaPoolTable.tableName,
        rowType: "GachaPool",
        shape: "keyed",
        primaryKey: { name: "id", type: "number" },
        indexes: [
        ],
    };

    private constructor(
        private readonly _keys: number[],
        private readonly _rows: Map<number, GachaPool>,
    ) {}

    static decode(rows: GachaPool[]): GachaPoolTable {
        return new GachaPoolTable(
            rows.map((row) => row.id),
            decodeMapTable(rows, (row) => row.id),
        );
    }

    info(): SoraTableInfo {
        return GachaPoolTable.tableInfo;
    }

    len(): number {
        return this._rows.size;
    }
    get(key: number): GachaPool | undefined {
        return this._rows.get(key);
    }

    rows(): ReadonlyMap<number, GachaPool> {
        return this._rows;
    }

    keys(): readonly number[] {
        return this._keys;
    }

    orderedRows(): GachaPool[] {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }
}
