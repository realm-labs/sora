// This file was generated by Sora. Do not edit manually.

import type { SoraKeyedTable, SoraTableInfo, SoraReader, SoraValue, TextKey } from "./sora_runtime.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";

import type { EventCondition } from "./event_condition.js";
import { collectEventConditionTextKeys, decodeEventCondition, decodeEventConditionValue } from "./event_condition.js";


export interface ComplexConditionGroup {
    id: number;
    name: string;
    /** A derived list of union values; each child row is edited without JSON */
    conditions: EventCondition[];
}

export function decodeComplexConditionGroup(reader: SoraReader): ComplexConditionGroup {
    return {
        id: reader.readI32(),
        name: reader.readString(),
        conditions: reader.readList(() => decodeEventCondition(reader)),
    };
}

export function decodeComplexConditionGroupValue(value: SoraValue): ComplexConditionGroup {
    const object = value.asObject();
    return {
        id: object.get("id").asInt(),
        name: object.get("name").asString(),
        conditions: object.get("conditions").asList((item) => decodeEventConditionValue(item)),
    };
}

export function collectComplexConditionGroupTextKeys(value: ComplexConditionGroup, out: TextKey[]): void {
    for (const item of value.conditions) { collectEventConditionTextKeys(item, out); }
}

export class ComplexConditionGroupTable implements SoraKeyedTable<number, ComplexConditionGroup> {
    static readonly tableName = "ComplexConditionGroup";
    static readonly tableInfo: SoraTableInfo = {
        name: ComplexConditionGroupTable.tableName,
        rowType: "ComplexConditionGroup",
        shape: "keyed",
        primaryKey: { name: "id", type: "number" },
        indexes: [
        ],
    };

    private constructor(
        private readonly _keys: number[],
        private readonly _rows: Map<number, ComplexConditionGroup>,
    ) {}

    static decode(rows: ComplexConditionGroup[]): ComplexConditionGroupTable {
        return new ComplexConditionGroupTable(
            rows.map((row) => row.id),
            decodeMapTable(rows, (row) => row.id),
        );
    }

    info(): SoraTableInfo {
        return ComplexConditionGroupTable.tableInfo;
    }

    len(): number {
        return this._rows.size;
    }
    get(key: number): ComplexConditionGroup | undefined {
        return this._rows.get(key);
    }

    rows(): ReadonlyMap<number, ComplexConditionGroup> {
        return this._rows;
    }

    keys(): readonly number[] {
        return this._keys;
    }

    orderedRows(): ComplexConditionGroup[] {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }
}
