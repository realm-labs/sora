// This file was generated by Sora. Do not edit manually.

import type { SoraKeyedTable, SoraTableInfo, SoraReader, SoraValue, TextKey } from "./sora_runtime.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";

import type { QuestType } from "./quest_type.js";
import { collectQuestTypeTextKeys, decodeQuestType, decodeQuestTypeValue } from "./quest_type.js";

import type { Reward } from "./reward.js";
import { collectRewardTextKeys, decodeReward, decodeRewardValue } from "./reward.js";

import type { Vec3 } from "./vec3.js";
import { collectVec3TextKeys, decodeVec3, decodeVec3Value } from "./vec3.js";


export interface Quest {
    readonly id: number;
    readonly questType: QuestType;
    readonly title: string;
    readonly requiredItem: number;
    readonly unlockSkills: readonly number[];
    readonly startPos: Vec3;
    /** Materialized from QuestReward child rows */
    readonly rewards: readonly Reward[];
}

export function decodeQuest(reader: SoraReader): Quest {
    return {
        id: reader.readI32(),
        questType: decodeQuestType(reader),
        title: reader.readString(),
        requiredItem: reader.readI32(),
        unlockSkills: reader.readList(() => reader.readI32()),
        startPos: decodeVec3(reader),
        rewards: reader.readList(() => decodeReward(reader)),
    };
}

export function decodeQuestValue(value: SoraValue): Quest {
    const object = value.asObject();
    return {
        id: object.get("id").asInt(),
        questType: decodeQuestTypeValue(object.get("quest_type")),
        title: object.get("title").asString(),
        requiredItem: object.get("required_item").asInt(),
        unlockSkills: object.get("unlock_skills").asList((item) => item.asInt()),
        startPos: decodeVec3Value(object.get("start_pos")),
        rewards: object.get("rewards").asList((item) => decodeRewardValue(item)),
    };
}

export function collectQuestTextKeys(value: Quest, out: TextKey[]): void {
    collectVec3TextKeys(value.startPos, out);
    for (const item of value.rewards) { collectRewardTextKeys(item, out); }
}

export class QuestTable implements SoraKeyedTable<number, Quest> {
    static readonly tableName = "Quest";
    static readonly tableInfo: SoraTableInfo = {
        name: QuestTable.tableName,
        rowType: "Quest",
        shape: "keyed",
        primaryKey: { name: "id", type: "number" },
        indexes: [
        ],
    };

    private constructor(
        private readonly _keys: readonly number[],
        private readonly _rows: ReadonlyMap<number, Quest>,
    ) {}

    static decode(rows: Quest[]): QuestTable {
        return new QuestTable(
            rows.map((row) => row.id),
            decodeMapTable(rows, (row) => row.id),
        );
    }

    info(): SoraTableInfo {
        return QuestTable.tableInfo;
    }

    get size(): number {
        return this._rows.size;
    }
    get(key: number): Quest | undefined {
        return this._rows.get(key);
    }

    has(key: number): boolean {
        return this._rows.has(key);
    }

    get rows(): ReadonlyMap<number, Quest> {
        return this._rows;
    }

    get orderedKeys(): readonly number[] {
        return this._keys;
    }

    get orderedRows(): readonly Quest[] {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }

    [Symbol.iterator](): Iterator<Quest> {
        return this._rows.values();
    }
}
