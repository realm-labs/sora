// This file was generated by Sora. Do not edit manually.

import type { SoraKeyedTable, SoraTableInfo, SoraReader, SoraValue, TextKey } from "./sora_runtime.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";

import type { ComplexBudget } from "./complex_budget.js";
import { collectComplexBudgetTextKeys, decodeComplexBudget, decodeComplexBudgetValue } from "./complex_budget.js";

import type { EventCondition } from "./event_condition.js";
import { collectEventConditionTextKeys, decodeEventCondition, decodeEventConditionValue } from "./event_condition.js";

import type { RewardAction } from "./reward_action.js";
import { collectRewardActionTextKeys, decodeRewardAction, decodeRewardActionValue } from "./reward_action.js";


export interface ComplexRule {
    id: number;
    name: string;
    /** Single union value derived from a tagged_columns child row */
    rootCondition: EventCondition;
    rootActionGroup: number;
    /** Non-JSON list<union<RewardAction>> assembled from child rows */
    actions: RewardAction[];
    /** Nested tuple, tuple_list, split, and map parsers in one cell */
    budget: ComplexBudget;
}

export function decodeComplexRule(reader: SoraReader): ComplexRule {
    return {
        id: reader.readI32(),
        name: reader.readString(),
        rootCondition: decodeEventCondition(reader),
        rootActionGroup: reader.readI32(),
        actions: reader.readList(() => decodeRewardAction(reader)),
        budget: decodeComplexBudget(reader),
    };
}

export function decodeComplexRuleValue(value: SoraValue): ComplexRule {
    const object = value.asObject();
    return {
        id: object.get("id").asInt(),
        name: object.get("name").asString(),
        rootCondition: decodeEventConditionValue(object.get("root_condition")),
        rootActionGroup: object.get("root_action_group").asInt(),
        actions: object.get("actions").asList((item) => decodeRewardActionValue(item)),
        budget: decodeComplexBudgetValue(object.get("budget")),
    };
}

export function collectComplexRuleTextKeys(value: ComplexRule, out: TextKey[]): void {
    collectEventConditionTextKeys(value.rootCondition, out);
    for (const item of value.actions) { collectRewardActionTextKeys(item, out); }
    collectComplexBudgetTextKeys(value.budget, out);
}

export class ComplexRuleTable implements SoraKeyedTable<number, ComplexRule> {
    static readonly tableName = "ComplexRule";
    static readonly tableInfo: SoraTableInfo = {
        name: ComplexRuleTable.tableName,
        rowType: "ComplexRule",
        shape: "keyed",
        primaryKey: { name: "id", type: "number" },
        indexes: [
        ],
    };

    private constructor(
        private readonly _keys: number[],
        private readonly _rows: Map<number, ComplexRule>,
    ) {}

    static decode(rows: ComplexRule[]): ComplexRuleTable {
        return new ComplexRuleTable(
            rows.map((row) => row.id),
            decodeMapTable(rows, (row) => row.id),
        );
    }

    info(): SoraTableInfo {
        return ComplexRuleTable.tableInfo;
    }

    len(): number {
        return this._rows.size;
    }
    get(key: number): ComplexRule | undefined {
        return this._rows.get(key);
    }

    rows(): ReadonlyMap<number, ComplexRule> {
        return this._rows;
    }

    keys(): readonly number[] {
        return this._keys;
    }

    orderedRows(): ComplexRule[] {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }
}
