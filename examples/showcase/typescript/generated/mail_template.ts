// This file was generated by Sora. Do not edit manually.

import type { SoraKeyedTable, SoraTableInfo, SoraReader, SoraValue, TextKey } from "./sora_runtime.js";
import { TextKey as RuntimeTextKey } from "./sora_runtime.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";

import type { MailType } from "./mail_type.js";
import { collectMailTypeTextKeys, decodeMailType, decodeMailTypeValue } from "./mail_type.js";

import type { Reward } from "./reward.js";
import { collectRewardTextKeys, decodeReward, decodeRewardValue } from "./reward.js";


export interface MailTemplate {
    readonly id: number;
    readonly mailType: MailType;
    readonly titleKey: TextKey;
    readonly bodyKey: TextKey;
    readonly rewards: readonly Reward[];
}

export function decodeMailTemplate(reader: SoraReader): MailTemplate {
    return {
        id: reader.readI32(),
        mailType: decodeMailType(reader),
        titleKey: new RuntimeTextKey(reader.readString()),
        bodyKey: new RuntimeTextKey(reader.readString()),
        rewards: reader.readList(() => decodeReward(reader)),
    };
}

export function decodeMailTemplateValue(value: SoraValue): MailTemplate {
    const object = value.asObject();
    return {
        id: object.get("id").asInt(),
        mailType: decodeMailTypeValue(object.get("mail_type")),
        titleKey: new RuntimeTextKey(object.get("title_key").asString()),
        bodyKey: new RuntimeTextKey(object.get("body_key").asString()),
        rewards: object.get("rewards").asList((item) => decodeRewardValue(item)),
    };
}

export function collectMailTemplateTextKeys(value: MailTemplate, out: TextKey[]): void {
    out.push(value.titleKey);
    out.push(value.bodyKey);
    for (const item of value.rewards) { collectRewardTextKeys(item, out); }
}

export class MailTemplateTable implements SoraKeyedTable<number, MailTemplate> {
    static readonly tableName = "MailTemplate";
    static readonly tableInfo: SoraTableInfo = {
        name: MailTemplateTable.tableName,
        rowType: "MailTemplate",
        shape: "keyed",
        primaryKey: { name: "id", type: "number" },
        indexes: [
        ],
    };

    private constructor(
        private readonly _keys: readonly number[],
        private readonly _rows: ReadonlyMap<number, MailTemplate>,
    ) {}

    static decode(rows: MailTemplate[]): MailTemplateTable {
        return new MailTemplateTable(
            rows.map((row) => row.id),
            decodeMapTable(rows, (row) => row.id),
        );
    }

    info(): SoraTableInfo {
        return MailTemplateTable.tableInfo;
    }

    get size(): number {
        return this._rows.size;
    }
    get(key: number): MailTemplate | undefined {
        return this._rows.get(key);
    }

    has(key: number): boolean {
        return this._rows.has(key);
    }

    get rows(): ReadonlyMap<number, MailTemplate> {
        return this._rows;
    }

    get orderedKeys(): readonly number[] {
        return this._keys;
    }

    get orderedRows(): readonly MailTemplate[] {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }

    [Symbol.iterator](): Iterator<MailTemplate> {
        return this._rows.values();
    }
}
