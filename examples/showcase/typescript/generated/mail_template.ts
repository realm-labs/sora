// This file was generated by Sora. Do not edit manually.

import type { SoraKeyedTable, SoraTableInfo, SoraReader, SoraValue } from "./sora_runtime.js";
import { TextKey } from "./sora_runtime.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";

import type { MailType } from "./mail_type.js";
import { decodeMailType, decodeMailTypeValue } from "./mail_type.js";

import type { Reward } from "./reward.js";
import { decodeReward, decodeRewardValue } from "./reward.js";


export interface MailTemplate {
    id: number;
    mailType: MailType;
    titleKey: TextKey;
    bodyKey: TextKey;
    rewards: Reward[];
}

export function decodeMailTemplate(reader: SoraReader): MailTemplate {
    return {
        id: reader.readI32(),
        mailType: decodeMailType(reader),
        titleKey: new TextKey(reader.readString()),
        bodyKey: new TextKey(reader.readString()),
        rewards: reader.readList(() => decodeReward(reader)),
    };
}

export function decodeMailTemplateValue(value: SoraValue): MailTemplate {
    const object = value.asObject();
    return {
        id: object.get("id").asInt(),
        mailType: decodeMailTypeValue(object.get("mail_type")),
        titleKey: new TextKey(object.get("title_key").asString()),
        bodyKey: new TextKey(object.get("body_key").asString()),
        rewards: object.get("rewards").asList((item) => decodeRewardValue(item)),
    };
}

export class MailTemplateTable implements SoraKeyedTable<number, MailTemplate> {
    static readonly tableName = "MailTemplate";
    static readonly tableInfo: SoraTableInfo = {
        name: MailTemplateTable.tableName,
        rowType: "MailTemplate",
        shape: "keyed",
        primaryKey: { name: "id", type: "number" },
        indexes: [
        ],
    };

    private constructor(
        private readonly _keys: number[],
        private readonly _rows: Map<number, MailTemplate>,
    ) {}

    static decode(rows: MailTemplate[]): MailTemplateTable {
        return new MailTemplateTable(
            rows.map((row) => row.id),
            decodeMapTable(rows, (row) => row.id),
        );
    }

    info(): SoraTableInfo {
        return MailTemplateTable.tableInfo;
    }

    len(): number {
        return this._rows.size;
    }
    get(key: number): MailTemplate | undefined {
        return this._rows.get(key);
    }

    rows(): ReadonlyMap<number, MailTemplate> {
        return this._rows;
    }

    keys(): readonly number[] {
        return this._keys;
    }

    orderedRows(): MailTemplate[] {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }
}
