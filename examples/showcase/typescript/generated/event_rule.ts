// This file was generated by Sora. Do not edit manually.

import type { SoraKeyedTable, SoraTableInfo, SoraReader, SoraValue, TextKey } from "./sora_runtime.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";

import type { EventCondition } from "./event_condition.js";
import { collectEventConditionTextKeys, decodeEventCondition, decodeEventConditionValue } from "./event_condition.js";

import type { RewardAction } from "./reward_action.js";
import { collectRewardActionTextKeys, decodeRewardAction, decodeRewardActionValue } from "./reward_action.js";


export interface EventRule {
    id: number;
    name: string;
    condition: EventCondition;
    actions: RewardAction[];
}

export function decodeEventRule(reader: SoraReader): EventRule {
    return {
        id: reader.readI32(),
        name: reader.readString(),
        condition: decodeEventCondition(reader),
        actions: reader.readList(() => decodeRewardAction(reader)),
    };
}

export function decodeEventRuleValue(value: SoraValue): EventRule {
    const object = value.asObject();
    return {
        id: object.get("id").asInt(),
        name: object.get("name").asString(),
        condition: decodeEventConditionValue(object.get("condition")),
        actions: object.get("actions").asList((item) => decodeRewardActionValue(item)),
    };
}

export function collectEventRuleTextKeys(value: EventRule, out: TextKey[]): void {
    collectEventConditionTextKeys(value.condition, out);
    for (const item of value.actions) { collectRewardActionTextKeys(item, out); }
}

export class EventRuleTable implements SoraKeyedTable<number, EventRule> {
    static readonly tableName = "EventRule";
    static readonly tableInfo: SoraTableInfo = {
        name: EventRuleTable.tableName,
        rowType: "EventRule",
        shape: "keyed",
        primaryKey: { name: "id", type: "number" },
        indexes: [
        ],
    };

    private constructor(
        private readonly _keys: number[],
        private readonly _rows: Map<number, EventRule>,
    ) {}

    static decode(rows: EventRule[]): EventRuleTable {
        return new EventRuleTable(
            rows.map((row) => row.id),
            decodeMapTable(rows, (row) => row.id),
        );
    }

    info(): SoraTableInfo {
        return EventRuleTable.tableInfo;
    }

    len(): number {
        return this._rows.size;
    }
    get(key: number): EventRule | undefined {
        return this._rows.get(key);
    }

    rows(): ReadonlyMap<number, EventRule> {
        return this._rows;
    }

    keys(): readonly number[] {
        return this._keys;
    }

    orderedRows(): EventRule[] {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }
}
