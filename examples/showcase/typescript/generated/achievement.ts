// This file was generated by Sora. Do not edit manually.

import type { SoraKeyedTable, SoraTableInfo, SoraReader, SoraValue, TextKey } from "./sora_runtime.js";
import { TextKey as RuntimeTextKey } from "./sora_runtime.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";

import type { ResourceCost } from "./resource_cost.js";
import { collectResourceCostTextKeys, decodeResourceCost, decodeResourceCostValue } from "./resource_cost.js";


export interface Achievement {
    id: number;
    titleKey: TextKey;
    targetCount: bigint;
    reward: ResourceCost;
}

export function decodeAchievement(reader: SoraReader): Achievement {
    return {
        id: reader.readI32(),
        titleKey: new RuntimeTextKey(reader.readString()),
        targetCount: reader.readI64(),
        reward: decodeResourceCost(reader),
    };
}

export function decodeAchievementValue(value: SoraValue): Achievement {
    const object = value.asObject();
    return {
        id: object.get("id").asInt(),
        titleKey: new RuntimeTextKey(object.get("title_key").asString()),
        targetCount: object.get("target_count").asBigInt(),
        reward: decodeResourceCostValue(object.get("reward")),
    };
}

export function collectAchievementTextKeys(value: Achievement, out: TextKey[]): void {
    out.push(value.titleKey);
    collectResourceCostTextKeys(value.reward, out);
}

export class AchievementTable implements SoraKeyedTable<number, Achievement> {
    static readonly tableName = "Achievement";
    static readonly tableInfo: SoraTableInfo = {
        name: AchievementTable.tableName,
        rowType: "Achievement",
        shape: "keyed",
        primaryKey: { name: "id", type: "number" },
        indexes: [
        ],
    };

    private constructor(
        private readonly _keys: number[],
        private readonly _rows: Map<number, Achievement>,
    ) {}

    static decode(rows: Achievement[]): AchievementTable {
        return new AchievementTable(
            rows.map((row) => row.id),
            decodeMapTable(rows, (row) => row.id),
        );
    }

    info(): SoraTableInfo {
        return AchievementTable.tableInfo;
    }

    len(): number {
        return this._rows.size;
    }
    get(key: number): Achievement | undefined {
        return this._rows.get(key);
    }

    rows(): ReadonlyMap<number, Achievement> {
        return this._rows;
    }

    keys(): readonly number[] {
        return this._keys;
    }

    orderedRows(): Achievement[] {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }
}
