// This file was generated by Sora. Do not edit manually.

import type { SoraReader, SoraValue, TextKey } from "./sora_runtime.js";

export type StatType =
    | "Hp"
    | "Attack"
    | "Defense"
    | "Speed"
    | "CritRate";

export const StatType = {
    Hp: "Hp",
    Attack: "Attack",
    Defense: "Defense",
    Speed: "Speed",
    CritRate: "CritRate",
} as const;
const values = new Map<number, StatType>([
    [0, StatType.Hp],
    [1, StatType.Attack],
    [2, StatType.Defense],
    [3, StatType.Speed],
    [4, StatType.CritRate],
]);

export function decodeStatType(reader: SoraReader): StatType {
    const id = reader.readU32();
    const value = values.get(id);
    if (value === undefined) {
        throw new Error(`invalid enum id ${id} for StatType`);
    }
    return value;
}

export function decodeStatTypeValue(value: SoraValue): StatType {
    const name = value.asString();
    if (![...values.values()].includes(name as StatType)) {
        throw new Error(`invalid enum value ${name} for StatType`);
    }
    return name as StatType;
}

export function collectStatTypeTextKeys(_value: StatType, _out: TextKey[]): void {}
