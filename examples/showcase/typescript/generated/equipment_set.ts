// This file was generated by Sora. Do not edit manually.

import type { SoraKeyedTable, SoraTableInfo, SoraReader, SoraValue, TextKey } from "./sora_runtime.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";

import type { SkillEffect } from "./skill_effect.js";
import { collectSkillEffectTextKeys, decodeSkillEffect, decodeSkillEffectValue } from "./skill_effect.js";


export interface EquipmentSet {
    readonly id: number;
    readonly name: string;
    readonly itemIds: readonly number[];
    readonly bonusEffect: SkillEffect;
}

export function decodeEquipmentSet(reader: SoraReader): EquipmentSet {
    return {
        id: reader.readI32(),
        name: reader.readString(),
        itemIds: reader.readList(() => reader.readI32()),
        bonusEffect: decodeSkillEffect(reader),
    };
}

export function decodeEquipmentSetValue(value: SoraValue): EquipmentSet {
    const object = value.asObject();
    return {
        id: object.get("id").asInt(),
        name: object.get("name").asString(),
        itemIds: object.get("item_ids").asList((item) => item.asInt()),
        bonusEffect: decodeSkillEffectValue(object.get("bonus_effect")),
    };
}

export function collectEquipmentSetTextKeys(value: EquipmentSet, out: TextKey[]): void {
    collectSkillEffectTextKeys(value.bonusEffect, out);
}

export class EquipmentSetTable implements SoraKeyedTable<number, EquipmentSet> {
    static readonly tableName = "EquipmentSet";
    static readonly tableInfo: SoraTableInfo = {
        name: EquipmentSetTable.tableName,
        rowType: "EquipmentSet",
        shape: "keyed",
        primaryKey: { name: "id", type: "number" },
        indexes: [
        ],
    };

    private constructor(
        private readonly _keys: readonly number[],
        private readonly _rows: ReadonlyMap<number, EquipmentSet>,
    ) {}

    static decode(rows: EquipmentSet[]): EquipmentSetTable {
        return new EquipmentSetTable(
            rows.map((row) => row.id),
            decodeMapTable(rows, (row) => row.id),
        );
    }

    info(): SoraTableInfo {
        return EquipmentSetTable.tableInfo;
    }

    get size(): number {
        return this._rows.size;
    }
    get(key: number): EquipmentSet | undefined {
        return this._rows.get(key);
    }

    has(key: number): boolean {
        return this._rows.has(key);
    }

    get rows(): ReadonlyMap<number, EquipmentSet> {
        return this._rows;
    }

    get orderedKeys(): readonly number[] {
        return this._keys;
    }

    get orderedRows(): readonly EquipmentSet[] {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }

    [Symbol.iterator](): Iterator<EquipmentSet> {
        return this._rows.values();
    }
}
