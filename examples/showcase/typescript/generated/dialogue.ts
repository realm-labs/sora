// This file was generated by Sora. Do not edit manually.

import type { SoraKeyedTable, SoraTableInfo, SoraReader, SoraValue, TextKey } from "./sora_runtime.js";
import { TextKey as RuntimeTextKey } from "./sora_runtime.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";


export interface Dialogue {
    readonly id: number;
    readonly speakerKey: TextKey;
    readonly lines: readonly string[];
}

export function decodeDialogue(reader: SoraReader): Dialogue {
    return {
        id: reader.readI32(),
        speakerKey: new RuntimeTextKey(reader.readString()),
        lines: reader.readList(() => reader.readString()),
    };
}

export function decodeDialogueValue(value: SoraValue): Dialogue {
    const object = value.asObject();
    return {
        id: object.get("id").asInt(),
        speakerKey: new RuntimeTextKey(object.get("speaker_key").asString()),
        lines: object.get("lines").asList((item) => item.asString()),
    };
}

export function collectDialogueTextKeys(value: Dialogue, out: TextKey[]): void {
    out.push(value.speakerKey);
}

export class DialogueTable implements SoraKeyedTable<number, Dialogue> {
    static readonly tableName = "Dialogue";
    static readonly tableInfo: SoraTableInfo = {
        name: DialogueTable.tableName,
        rowType: "Dialogue",
        shape: "keyed",
        primaryKey: { name: "id", type: "number" },
        indexes: [
        ],
    };

    private constructor(
        private readonly _keys: readonly number[],
        private readonly _rows: ReadonlyMap<number, Dialogue>,
    ) {}

    static decode(rows: Dialogue[]): DialogueTable {
        return new DialogueTable(
            rows.map((row) => row.id),
            decodeMapTable(rows, (row) => row.id),
        );
    }

    info(): SoraTableInfo {
        return DialogueTable.tableInfo;
    }

    get size(): number {
        return this._rows.size;
    }
    get(key: number): Dialogue | undefined {
        return this._rows.get(key);
    }

    has(key: number): boolean {
        return this._rows.has(key);
    }

    get rows(): ReadonlyMap<number, Dialogue> {
        return this._rows;
    }

    get orderedKeys(): readonly number[] {
        return this._keys;
    }

    get orderedRows(): readonly Dialogue[] {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }

    [Symbol.iterator](): Iterator<Dialogue> {
        return this._rows.values();
    }
}
