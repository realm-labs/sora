// This file was generated by Sora. Do not edit manually.

import type { SoraKeyedTable, SoraTableInfo, SoraReader, SoraValue, TextKey } from "./sora_runtime.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";

import type { StatModifier } from "./stat_modifier.js";
import { collectStatModifierTextKeys, decodeStatModifier, decodeStatModifierValue } from "./stat_modifier.js";


export interface Buff {
    readonly id: number;
    readonly name: string;
    readonly duration: bigint;
    readonly modifiers: readonly StatModifier[];
}

export function decodeBuff(reader: SoraReader): Buff {
    return {
        id: reader.readI32(),
        name: reader.readString(),
        duration: reader.readI64(),
        modifiers: reader.readList(() => decodeStatModifier(reader)),
    };
}

export function decodeBuffValue(value: SoraValue): Buff {
    const object = value.asObject();
    return {
        id: object.get("id").asInt(),
        name: object.get("name").asString(),
        duration: object.get("duration").asBigInt(),
        modifiers: object.get("modifiers").asList((item) => decodeStatModifierValue(item)),
    };
}

export function collectBuffTextKeys(value: Buff, out: TextKey[]): void {
    for (const item of value.modifiers) { collectStatModifierTextKeys(item, out); }
}

export class BuffTable implements SoraKeyedTable<number, Buff> {
    static readonly tableName = "Buff";
    static readonly tableInfo: SoraTableInfo = {
        name: BuffTable.tableName,
        rowType: "Buff",
        shape: "keyed",
        primaryKey: { name: "id", type: "number" },
        indexes: [
        ],
    };

    private constructor(
        private readonly _keys: readonly number[],
        private readonly _rows: ReadonlyMap<number, Buff>,
    ) {}

    static decode(rows: Buff[]): BuffTable {
        return new BuffTable(
            rows.map((row) => row.id),
            decodeMapTable(rows, (row) => row.id),
        );
    }

    info(): SoraTableInfo {
        return BuffTable.tableInfo;
    }

    get size(): number {
        return this._rows.size;
    }
    get(key: number): Buff | undefined {
        return this._rows.get(key);
    }

    has(key: number): boolean {
        return this._rows.has(key);
    }

    get rows(): ReadonlyMap<number, Buff> {
        return this._rows;
    }

    get orderedKeys(): readonly number[] {
        return this._keys;
    }

    get orderedRows(): readonly Buff[] {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }

    [Symbol.iterator](): Iterator<Buff> {
        return this._rows.values();
    }
}
