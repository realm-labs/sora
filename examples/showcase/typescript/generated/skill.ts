// This file was generated by Sora. Do not edit manually.

import type { SoraConfigTable, SoraReader, SoraValue } from "./sora_runtime.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";

import type { ElementType } from "./element_type.js";
import { decodeElementType, decodeElementTypeValue } from "./element_type.js";

import type { ResourceCost } from "./resource_cost.js";
import { decodeResourceCost, decodeResourceCostValue } from "./resource_cost.js";

import type { SkillEffect } from "./skill_effect.js";
import { decodeSkillEffect, decodeSkillEffectValue } from "./skill_effect.js";

import type { Vec3 } from "./vec3.js";
import { decodeVec3, decodeVec3Value } from "./vec3.js";


export interface Skill {
    id: number;
    name: string;
    element: ElementType;
    /** Tuple cost, e.g. Gold,0,150 */
    cost: ResourceCost;
    /** JSON object with element/power/radius */
    effect: SkillEffect;
    requiredLevel: number;
    /** Optional item requirement */
    requiredItem: number | undefined;
    castOrigin: Vec3;
}

export function decodeSkill(reader: SoraReader): Skill {
    return {
        id: reader.readI32(),
        name: reader.readString(),
        element: decodeElementType(reader),
        cost: decodeResourceCost(reader),
        effect: decodeSkillEffect(reader),
        requiredLevel: reader.readI32(),
        requiredItem: reader.readOptional(() => reader.readI32()),
        castOrigin: decodeVec3(reader),
    };
}

export function decodeSkillValue(value: SoraValue): Skill {
    const object = value.asObject();
    return {
        id: object.get("id").asInt(),
        name: object.get("name").asString(),
        element: decodeElementTypeValue(object.get("element")),
        cost: decodeResourceCostValue(object.get("cost")),
        effect: decodeSkillEffectValue(object.get("effect")),
        requiredLevel: object.get("required_level").asInt(),
        requiredItem: object.get("required_item").isNull() ? undefined : object.get("required_item").asInt(),
        castOrigin: decodeVec3Value(object.get("cast_origin")),
    };
}

export class SkillTable implements SoraConfigTable {
    private constructor(
        private readonly _rows: Map<number, Skill>,
        private readonly _keys: number[],
    ) {}

    static decode(rows: Skill[]): SkillTable {
        return new SkillTable(
            decodeMapTable(rows, (row) => row.id),
            rows.map((row) => row.id),
        );
    }

    name(): string {
        return "Skill";
    }

    mode(): string {
        return "map";
    }

    key(): string | undefined {
        return "id";
    }

    len(): number {
        return this._rows.size;
    }
    get(key: number): Skill | undefined {
        return this._rows.get(key);
    }

    rows(): ReadonlyMap<number, Skill> {
        return this._rows;
    }

    keys(): readonly number[] {
        return this._keys;
    }

    orderedRows(): Skill[] {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }
}
