// This file was generated by Sora. Do not edit manually.

import type { SoraKeyedTable, SoraTableInfo, SoraReader, SoraValue } from "./sora_runtime.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";

import type { RewardAction } from "./reward_action.js";
import { decodeRewardAction, decodeRewardActionValue } from "./reward_action.js";


export interface ComplexActionGroup {
    id: number;
    name: string;
    actions: RewardAction[];
}

export function decodeComplexActionGroup(reader: SoraReader): ComplexActionGroup {
    return {
        id: reader.readI32(),
        name: reader.readString(),
        actions: reader.readList(() => decodeRewardAction(reader)),
    };
}

export function decodeComplexActionGroupValue(value: SoraValue): ComplexActionGroup {
    const object = value.asObject();
    return {
        id: object.get("id").asInt(),
        name: object.get("name").asString(),
        actions: object.get("actions").asList((item) => decodeRewardActionValue(item)),
    };
}

export class ComplexActionGroupTable implements SoraKeyedTable<number, ComplexActionGroup> {
    static readonly tableName = "ComplexActionGroup";
    static readonly tableInfo: SoraTableInfo = {
        name: ComplexActionGroupTable.tableName,
        rowType: "ComplexActionGroup",
        shape: "keyed",
        primaryKey: { name: "id", type: "number" },
        indexes: [
        ],
    };

    private constructor(
        private readonly _keys: number[],
        private readonly _rows: Map<number, ComplexActionGroup>,
    ) {}

    static decode(rows: ComplexActionGroup[]): ComplexActionGroupTable {
        return new ComplexActionGroupTable(
            rows.map((row) => row.id),
            decodeMapTable(rows, (row) => row.id),
        );
    }

    info(): SoraTableInfo {
        return ComplexActionGroupTable.tableInfo;
    }

    len(): number {
        return this._rows.size;
    }
    get(key: number): ComplexActionGroup | undefined {
        return this._rows.get(key);
    }

    rows(): ReadonlyMap<number, ComplexActionGroup> {
        return this._rows;
    }

    keys(): readonly number[] {
        return this._keys;
    }

    orderedRows(): ComplexActionGroup[] {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }
}
