// This file was generated by Sora. Do not edit manually.

import type { SoraKeyedTable, SoraTableInfo, SoraReader, SoraValue, TextKey } from "./sora_runtime.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";

import type { RewardAction } from "./reward_action.js";
import { collectRewardActionTextKeys, decodeRewardAction, decodeRewardActionValue } from "./reward_action.js";


export interface ComplexActionEntry {
    id: number;
    groupId: number;
    seq: number;
    value: RewardAction;
}

export function decodeComplexActionEntry(reader: SoraReader): ComplexActionEntry {
    return {
        id: reader.readI32(),
        groupId: reader.readI32(),
        seq: reader.readI32(),
        value: decodeRewardAction(reader),
    };
}

export function decodeComplexActionEntryValue(value: SoraValue): ComplexActionEntry {
    const object = value.asObject();
    return {
        id: object.get("id").asInt(),
        groupId: object.get("group_id").asInt(),
        seq: object.get("seq").asInt(),
        value: decodeRewardActionValue(object.get("value")),
    };
}

export function collectComplexActionEntryTextKeys(value: ComplexActionEntry, out: TextKey[]): void {
    collectRewardActionTextKeys(value.value, out);
}

export class ComplexActionEntryTable implements SoraKeyedTable<number, ComplexActionEntry> {
    static readonly tableName = "ComplexActionEntry";
    static readonly tableInfo: SoraTableInfo = {
        name: ComplexActionEntryTable.tableName,
        rowType: "ComplexActionEntry",
        shape: "keyed",
        primaryKey: { name: "id", type: "number" },
        indexes: [
        ],
    };

    private constructor(
        private readonly _keys: number[],
        private readonly _rows: Map<number, ComplexActionEntry>,
    ) {}

    static decode(rows: ComplexActionEntry[]): ComplexActionEntryTable {
        return new ComplexActionEntryTable(
            rows.map((row) => row.id),
            decodeMapTable(rows, (row) => row.id),
        );
    }

    info(): SoraTableInfo {
        return ComplexActionEntryTable.tableInfo;
    }

    len(): number {
        return this._rows.size;
    }
    get(key: number): ComplexActionEntry | undefined {
        return this._rows.get(key);
    }

    rows(): ReadonlyMap<number, ComplexActionEntry> {
        return this._rows;
    }

    keys(): readonly number[] {
        return this._keys;
    }

    orderedRows(): ComplexActionEntry[] {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }
}
