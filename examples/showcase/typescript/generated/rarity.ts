// This file was generated by Sora. Do not edit manually.

import type { SoraReader, SoraValue, TextKey } from "./sora_runtime.js";

export type Rarity =
    | "Common"
    | "Uncommon"
    | "Rare"
    | "Epic"
    | "Legendary";

export const Rarity = {
    Common: "Common",
    Uncommon: "Uncommon",
    Rare: "Rare",
    Epic: "Epic",
    Legendary: "Legendary",
} as const;
const values = new Map<number, Rarity>([
    [0, Rarity.Common],
    [1, Rarity.Uncommon],
    [2, Rarity.Rare],
    [3, Rarity.Epic],
    [4, Rarity.Legendary],
]);

export function decodeRarity(reader: SoraReader): Rarity {
    const id = reader.readU32();
    const value = values.get(id);
    if (value === undefined) {
        throw new Error(`invalid enum id ${id} for Rarity`);
    }
    return value;
}

export function decodeRarityValue(value: SoraValue): Rarity {
    const name = value.asString();
    if (![...values.values()].includes(name as Rarity)) {
        throw new Error(`invalid enum value ${name} for Rarity`);
    }
    return name as Rarity;
}

export function collectRarityTextKeys(_value: Rarity, _out: TextKey[]): void {}
