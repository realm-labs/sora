// This file was generated by Sora. Do not edit manually.

package com.sora.showcase;

import java.util.List;
import java.util.Map;
import java.util.Set;

public final class DungeonTable extends java.util.AbstractMap<Integer, Dungeon> implements SoraConfig.KeyedTable<Integer, Dungeon> {
    public static final String NAME = "Dungeon";
    public static final SoraConfig.TableInfo INFO = new SoraConfig.TableInfo(
        NAME,
        "Dungeon",
        SoraConfig.TableShape.KEYED,
        new SoraConfig.KeyInfo("id", "Integer"),
        List.of(
        )
    );
    private final List<Integer> keys;
    private final java.util.Map<Integer, Dungeon> rows;

    private DungeonTable(List<Integer> keys, java.util.Map<Integer, Dungeon> rows) {
        this.keys = List.copyOf(keys);
        this.rows = Map.copyOf(rows);
    }

    private static DungeonTable fromRows(List<Dungeon> rows) {
        return new DungeonTable(
            rows.stream().map(Dungeon::id).toList(),
            SoraConfig.decodeMapTable(rows, Dungeon::id)
        );
    }

    static DungeonTable decode(SoraTableSource source) {
        return fromRows(source.decodeTable(NAME, Dungeon::decode, Dungeon::decode));
    }

    public java.util.Map<Integer, Dungeon> rows() {
        return rows;
    }
    @Override
    @SoraNullable
    public Dungeon get(Object key) {
        return rows.get(key);
    }

    @Override
    public List<Integer> orderedKeys() {
        return keys;
    }

    @Override
    public List<Dungeon> orderedRows() {
        return keys.stream().map(rows::get).toList();
    }

    @Override
    public Set<Map.Entry<Integer, Dungeon>> entrySet() {
        return rows.entrySet();
    }
    @Override
    public SoraConfig.TableInfo info() {
        return INFO;
    }

    @Override
    public int size() {
        return rows.size();
    }
}
