// This file was generated by Sora. Do not edit manually.

package com.sora.showcase;

import java.util.List;
import java.util.Map;
import java.util.Set;

public final class LevelExpTable extends java.util.AbstractMap<Integer, LevelExp> implements SoraConfig.KeyedTable<Integer, LevelExp> {
    public static final String NAME = "LevelExp";
    public static final SoraConfig.TableInfo INFO = new SoraConfig.TableInfo(
        NAME,
        "LevelExp",
        SoraConfig.TableShape.KEYED,
        new SoraConfig.KeyInfo("level", "Integer"),
        List.of(
        )
    );
    private final List<Integer> keys;
    private final java.util.Map<Integer, LevelExp> rows;

    private LevelExpTable(List<Integer> keys, java.util.Map<Integer, LevelExp> rows) {
        this.keys = List.copyOf(keys);
        this.rows = Map.copyOf(rows);
    }

    private static LevelExpTable fromRows(List<LevelExp> rows) {
        return new LevelExpTable(
            rows.stream().map(LevelExp::level).toList(),
            SoraConfig.decodeMapTable(rows, LevelExp::level)
        );
    }

    static LevelExpTable decode(SoraTableSource source) {
        return fromRows(source.decodeTable(NAME, LevelExp::decode, LevelExp::decode));
    }

    public java.util.Map<Integer, LevelExp> rows() {
        return rows;
    }
    @Override
    @SoraNullable
    public LevelExp get(Object key) {
        return rows.get(key);
    }

    @Override
    public List<Integer> orderedKeys() {
        return keys;
    }

    @Override
    public List<LevelExp> orderedRows() {
        return keys.stream().map(rows::get).toList();
    }

    @Override
    public Set<Map.Entry<Integer, LevelExp>> entrySet() {
        return rows.entrySet();
    }
    @Override
    public SoraConfig.TableInfo info() {
        return INFO;
    }

    @Override
    public int size() {
        return rows.size();
    }
}
