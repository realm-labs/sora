// This file was generated by Sora. Do not edit manually.

package com.sora.showcase;

import java.util.List;
import java.util.Map;
import java.util.Set;

public final class AchievementTable extends java.util.AbstractMap<Integer, Achievement> implements SoraConfig.KeyedTable<Integer, Achievement> {
    public static final String NAME = "Achievement";
    public static final SoraConfig.TableInfo INFO = new SoraConfig.TableInfo(
        NAME,
        "Achievement",
        SoraConfig.TableShape.KEYED,
        new SoraConfig.KeyInfo("id", "Integer"),
        List.of(
        )
    );
    private final List<Integer> keys;
    private final java.util.Map<Integer, Achievement> rows;

    private AchievementTable(List<Integer> keys, java.util.Map<Integer, Achievement> rows) {
        this.keys = List.copyOf(keys);
        this.rows = Map.copyOf(rows);
    }

    private static AchievementTable fromRows(List<Achievement> rows) {
        return new AchievementTable(
            rows.stream().map(Achievement::id).toList(),
            SoraConfig.decodeMapTable(rows, Achievement::id)
        );
    }

    static AchievementTable decode(SoraTableSource source) {
        return fromRows(source.decodeTable(NAME, Achievement::decode, Achievement::decode));
    }

    public java.util.Map<Integer, Achievement> rows() {
        return rows;
    }
    @Override
    @SoraNullable
    public Achievement get(Object key) {
        return rows.get(key);
    }

    @Override
    public List<Integer> orderedKeys() {
        return keys;
    }

    @Override
    public List<Achievement> orderedRows() {
        return keys.stream().map(rows::get).toList();
    }

    @Override
    public Set<Map.Entry<Integer, Achievement>> entrySet() {
        return rows.entrySet();
    }
    @Override
    public SoraConfig.TableInfo info() {
        return INFO;
    }

    @Override
    public int size() {
        return rows.size();
    }
}
