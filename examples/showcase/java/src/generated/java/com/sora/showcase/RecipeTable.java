// This file was generated by Sora. Do not edit manually.

package com.sora.showcase;

import java.util.List;
import java.util.Map;
import java.util.Set;

public final class RecipeTable extends java.util.AbstractMap<Integer, Recipe> implements SoraConfig.KeyedTable<Integer, Recipe> {
    public static final String NAME = "Recipe";
    public static final SoraConfig.TableInfo INFO = new SoraConfig.TableInfo(
        NAME,
        "Recipe",
        SoraConfig.TableShape.KEYED,
        new SoraConfig.KeyInfo("id", "Integer"),
        List.of(
        )
    );
    private final List<Integer> keys;
    private final java.util.Map<Integer, Recipe> rows;

    private RecipeTable(List<Integer> keys, java.util.Map<Integer, Recipe> rows) {
        this.keys = List.copyOf(keys);
        this.rows = Map.copyOf(rows);
    }

    private static RecipeTable fromRows(List<Recipe> rows) {
        return new RecipeTable(
            rows.stream().map(Recipe::id).toList(),
            SoraConfig.decodeMapTable(rows, Recipe::id)
        );
    }

    static RecipeTable decode(SoraTableSource source) {
        return fromRows(source.decodeTable(NAME, Recipe::decode, Recipe::decode));
    }

    public java.util.Map<Integer, Recipe> rows() {
        return rows;
    }
    @Override
    @SoraNullable
    public Recipe get(Object key) {
        return rows.get(key);
    }

    @Override
    public List<Integer> orderedKeys() {
        return keys;
    }

    @Override
    public List<Recipe> orderedRows() {
        return keys.stream().map(rows::get).toList();
    }

    @Override
    public Set<Map.Entry<Integer, Recipe>> entrySet() {
        return rows.entrySet();
    }
    @Override
    public SoraConfig.TableInfo info() {
        return INFO;
    }

    @Override
    public int size() {
        return rows.size();
    }
}
