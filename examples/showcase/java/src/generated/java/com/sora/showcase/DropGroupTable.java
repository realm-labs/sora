// This file was generated by Sora. Do not edit manually.

package com.sora.showcase;

import java.util.List;
import java.util.Map;
import java.util.Set;

public final class DropGroupTable extends java.util.AbstractMap<Integer, DropGroup> implements SoraConfig.KeyedTable<Integer, DropGroup> {
    public static final String NAME = "DropGroup";
    public static final SoraConfig.TableInfo INFO = new SoraConfig.TableInfo(
        NAME,
        "DropGroup",
        SoraConfig.TableShape.KEYED,
        new SoraConfig.KeyInfo("id", "Integer"),
        List.of(
        )
    );
    private final List<Integer> keys;
    private final java.util.Map<Integer, DropGroup> rows;

    private DropGroupTable(List<Integer> keys, java.util.Map<Integer, DropGroup> rows) {
        this.keys = List.copyOf(keys);
        this.rows = Map.copyOf(rows);
    }

    private static DropGroupTable fromRows(List<DropGroup> rows) {
        return new DropGroupTable(
            rows.stream().map(DropGroup::id).toList(),
            SoraConfig.decodeMapTable(rows, DropGroup::id)
        );
    }

    static DropGroupTable decode(SoraTableSource source) {
        return fromRows(source.decodeTable(NAME, DropGroup::decode, DropGroup::decode));
    }

    public java.util.Map<Integer, DropGroup> rows() {
        return rows;
    }
    @Override
    @SoraNullable
    public DropGroup get(Object key) {
        return rows.get(key);
    }

    @Override
    public List<Integer> orderedKeys() {
        return keys;
    }

    @Override
    public List<DropGroup> orderedRows() {
        return keys.stream().map(rows::get).toList();
    }

    @Override
    public Set<Map.Entry<Integer, DropGroup>> entrySet() {
        return rows.entrySet();
    }
    @Override
    public SoraConfig.TableInfo info() {
        return INFO;
    }

    @Override
    public int size() {
        return rows.size();
    }
}
