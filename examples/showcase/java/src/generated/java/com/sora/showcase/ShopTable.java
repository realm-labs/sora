// This file was generated by Sora. Do not edit manually.

package com.sora.showcase;

import java.util.List;
import java.util.Map;
import java.util.Set;

public final class ShopTable extends java.util.AbstractMap<Integer, Shop> implements SoraConfig.KeyedTable<Integer, Shop> {
    public static final String NAME = "Shop";
    public static final SoraConfig.TableInfo INFO = new SoraConfig.TableInfo(
        NAME,
        "Shop",
        SoraConfig.TableShape.KEYED,
        new SoraConfig.KeyInfo("id", "Integer"),
        List.of(
        )
    );
    private final List<Integer> keys;
    private final java.util.Map<Integer, Shop> rows;

    private ShopTable(List<Integer> keys, java.util.Map<Integer, Shop> rows) {
        this.keys = List.copyOf(keys);
        this.rows = Map.copyOf(rows);
    }

    private static ShopTable fromRows(List<Shop> rows) {
        return new ShopTable(
            rows.stream().map(Shop::id).toList(),
            SoraConfig.decodeMapTable(rows, Shop::id)
        );
    }

    static ShopTable decode(SoraTableSource source) {
        return fromRows(source.decodeTable(NAME, Shop::decode, Shop::decode));
    }

    public java.util.Map<Integer, Shop> rows() {
        return rows;
    }
    @Override
    @SoraNullable
    public Shop get(Object key) {
        return rows.get(key);
    }

    @Override
    public List<Integer> orderedKeys() {
        return keys;
    }

    @Override
    public List<Shop> orderedRows() {
        return keys.stream().map(rows::get).toList();
    }

    @Override
    public Set<Map.Entry<Integer, Shop>> entrySet() {
        return rows.entrySet();
    }
    @Override
    public SoraConfig.TableInfo info() {
        return INFO;
    }

    @Override
    public int size() {
        return rows.size();
    }
}
