// This file was generated by Sora. Do not edit manually.

package com.sora.showcase;

import java.util.List;
import java.util.Map;
import java.util.Set;

public final class VipLevelTable extends java.util.AbstractMap<Integer, VipLevel> implements SoraConfig.KeyedTable<Integer, VipLevel> {
    public static final String NAME = "VipLevel";
    public static final SoraConfig.TableInfo INFO = new SoraConfig.TableInfo(
        NAME,
        "VipLevel",
        SoraConfig.TableShape.KEYED,
        new SoraConfig.KeyInfo("level", "Integer"),
        List.of(
        )
    );
    private final List<Integer> keys;
    private final java.util.Map<Integer, VipLevel> rows;

    private VipLevelTable(List<Integer> keys, java.util.Map<Integer, VipLevel> rows) {
        this.keys = List.copyOf(keys);
        this.rows = Map.copyOf(rows);
    }

    private static VipLevelTable fromRows(List<VipLevel> rows) {
        return new VipLevelTable(
            rows.stream().map(VipLevel::level).toList(),
            SoraConfig.decodeMapTable(rows, VipLevel::level)
        );
    }

    static VipLevelTable decode(SoraTableSource source) {
        return fromRows(source.decodeTable(NAME, VipLevel::decode, VipLevel::decode));
    }

    public java.util.Map<Integer, VipLevel> rows() {
        return rows;
    }
    @Override
    @SoraNullable
    public VipLevel get(Object key) {
        return rows.get(key);
    }

    @Override
    public List<Integer> orderedKeys() {
        return keys;
    }

    @Override
    public List<VipLevel> orderedRows() {
        return keys.stream().map(rows::get).toList();
    }

    @Override
    public Set<Map.Entry<Integer, VipLevel>> entrySet() {
        return rows.entrySet();
    }
    @Override
    public SoraConfig.TableInfo info() {
        return INFO;
    }

    @Override
    public int size() {
        return rows.size();
    }
}
