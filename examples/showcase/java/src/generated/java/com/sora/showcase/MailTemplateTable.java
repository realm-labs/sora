// This file was generated by Sora. Do not edit manually.

package com.sora.showcase;

import java.util.List;
import java.util.Map;
import java.util.Set;

public final class MailTemplateTable extends java.util.AbstractMap<Integer, MailTemplate> implements SoraConfig.KeyedTable<Integer, MailTemplate> {
    public static final String NAME = "MailTemplate";
    public static final SoraConfig.TableInfo INFO = new SoraConfig.TableInfo(
        NAME,
        "MailTemplate",
        SoraConfig.TableShape.KEYED,
        new SoraConfig.KeyInfo("id", "Integer"),
        List.of(
        )
    );
    private final List<Integer> keys;
    private final java.util.Map<Integer, MailTemplate> rows;

    private MailTemplateTable(List<Integer> keys, java.util.Map<Integer, MailTemplate> rows) {
        this.keys = List.copyOf(keys);
        this.rows = Map.copyOf(rows);
    }

    private static MailTemplateTable fromRows(List<MailTemplate> rows) {
        return new MailTemplateTable(
            rows.stream().map(MailTemplate::id).toList(),
            SoraConfig.decodeMapTable(rows, MailTemplate::id)
        );
    }

    static MailTemplateTable decode(SoraTableSource source) {
        return fromRows(source.decodeTable(NAME, MailTemplate::decode, MailTemplate::decode));
    }

    public java.util.Map<Integer, MailTemplate> rows() {
        return rows;
    }
    @Override
    @SoraNullable
    public MailTemplate get(Object key) {
        return rows.get(key);
    }

    @Override
    public List<Integer> orderedKeys() {
        return keys;
    }

    @Override
    public List<MailTemplate> orderedRows() {
        return keys.stream().map(rows::get).toList();
    }

    @Override
    public Set<Map.Entry<Integer, MailTemplate>> entrySet() {
        return rows.entrySet();
    }
    @Override
    public SoraConfig.TableInfo info() {
        return INFO;
    }

    @Override
    public int size() {
        return rows.size();
    }
}
