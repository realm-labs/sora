// This file was generated by Sora. Do not edit manually.

package com.sora.showcase;

import java.util.List;
import java.util.Map;
import java.util.Set;

public final class QuestTable extends java.util.AbstractMap<Integer, Quest> implements SoraConfig.KeyedTable<Integer, Quest> {
    public static final String NAME = "Quest";
    public static final SoraConfig.TableInfo INFO = new SoraConfig.TableInfo(
        NAME,
        "Quest",
        SoraConfig.TableShape.KEYED,
        new SoraConfig.KeyInfo("id", "Integer"),
        List.of(
        )
    );
    private final List<Integer> keys;
    private final java.util.Map<Integer, Quest> rows;

    private QuestTable(List<Integer> keys, java.util.Map<Integer, Quest> rows) {
        this.keys = List.copyOf(keys);
        this.rows = Map.copyOf(rows);
    }

    private static QuestTable fromRows(List<Quest> rows) {
        return new QuestTable(
            rows.stream().map(Quest::id).toList(),
            SoraConfig.decodeMapTable(rows, Quest::id)
        );
    }

    static QuestTable decode(SoraTableSource source) {
        return fromRows(source.decodeTable(NAME, Quest::decode, Quest::decode));
    }

    public java.util.Map<Integer, Quest> rows() {
        return rows;
    }
    @Override
    @SoraNullable
    public Quest get(Object key) {
        return rows.get(key);
    }

    @Override
    public List<Integer> orderedKeys() {
        return keys;
    }

    @Override
    public List<Quest> orderedRows() {
        return keys.stream().map(rows::get).toList();
    }

    @Override
    public Set<Map.Entry<Integer, Quest>> entrySet() {
        return rows.entrySet();
    }
    @Override
    public SoraConfig.TableInfo info() {
        return INFO;
    }

    @Override
    public int size() {
        return rows.size();
    }
}
