// This file was generated by Sora. Do not edit manually.

package com.sora.showcase;

import java.util.List;
import java.util.Map;
import java.util.Set;

public final class MonsterTable extends java.util.AbstractMap<Integer, Monster> implements SoraConfig.KeyedTable<Integer, Monster> {
    public static final String NAME = "Monster";
    public static final SoraConfig.TableInfo INFO = new SoraConfig.TableInfo(
        NAME,
        "Monster",
        SoraConfig.TableShape.KEYED,
        new SoraConfig.KeyInfo("id", "Integer"),
        List.of(
        )
    );
    private final List<Integer> keys;
    private final java.util.Map<Integer, Monster> rows;

    private MonsterTable(List<Integer> keys, java.util.Map<Integer, Monster> rows) {
        this.keys = List.copyOf(keys);
        this.rows = Map.copyOf(rows);
    }

    private static MonsterTable fromRows(List<Monster> rows) {
        return new MonsterTable(
            rows.stream().map(Monster::id).toList(),
            SoraConfig.decodeMapTable(rows, Monster::id)
        );
    }

    static MonsterTable decode(SoraTableSource source) {
        return fromRows(source.decodeTable(NAME, Monster::decode, Monster::decode));
    }

    public java.util.Map<Integer, Monster> rows() {
        return rows;
    }
    @Override
    @SoraNullable
    public Monster get(Object key) {
        return rows.get(key);
    }

    @Override
    public List<Integer> orderedKeys() {
        return keys;
    }

    @Override
    public List<Monster> orderedRows() {
        return keys.stream().map(rows::get).toList();
    }

    @Override
    public Set<Map.Entry<Integer, Monster>> entrySet() {
        return rows.entrySet();
    }
    @Override
    public SoraConfig.TableInfo info() {
        return INFO;
    }

    @Override
    public int size() {
        return rows.size();
    }
}
