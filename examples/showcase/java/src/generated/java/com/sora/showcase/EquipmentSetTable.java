// This file was generated by Sora. Do not edit manually.

package com.sora.showcase;

import java.util.List;
import java.util.Map;
import java.util.Set;

public final class EquipmentSetTable extends java.util.AbstractMap<Integer, EquipmentSet> implements SoraConfig.KeyedTable<Integer, EquipmentSet> {
    public static final String NAME = "EquipmentSet";
    public static final SoraConfig.TableInfo INFO = new SoraConfig.TableInfo(
        NAME,
        "EquipmentSet",
        SoraConfig.TableShape.KEYED,
        new SoraConfig.KeyInfo("id", "Integer"),
        List.of(
        )
    );
    private final List<Integer> keys;
    private final java.util.Map<Integer, EquipmentSet> rows;

    private EquipmentSetTable(List<Integer> keys, java.util.Map<Integer, EquipmentSet> rows) {
        this.keys = List.copyOf(keys);
        this.rows = Map.copyOf(rows);
    }

    private static EquipmentSetTable fromRows(List<EquipmentSet> rows) {
        return new EquipmentSetTable(
            rows.stream().map(EquipmentSet::id).toList(),
            SoraConfig.decodeMapTable(rows, EquipmentSet::id)
        );
    }

    static EquipmentSetTable decode(SoraTableSource source) {
        return fromRows(source.decodeTable(NAME, EquipmentSet::decode, EquipmentSet::decode));
    }

    public java.util.Map<Integer, EquipmentSet> rows() {
        return rows;
    }
    @Override
    @SoraNullable
    public EquipmentSet get(Object key) {
        return rows.get(key);
    }

    @Override
    public List<Integer> orderedKeys() {
        return keys;
    }

    @Override
    public List<EquipmentSet> orderedRows() {
        return keys.stream().map(rows::get).toList();
    }

    @Override
    public Set<Map.Entry<Integer, EquipmentSet>> entrySet() {
        return rows.entrySet();
    }
    @Override
    public SoraConfig.TableInfo info() {
        return INFO;
    }

    @Override
    public int size() {
        return rows.size();
    }
}
