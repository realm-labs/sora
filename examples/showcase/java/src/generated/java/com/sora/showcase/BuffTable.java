// This file was generated by Sora. Do not edit manually.

package com.sora.showcase;

import java.util.List;
import java.util.Map;
import java.util.Set;

public final class BuffTable extends java.util.AbstractMap<Integer, Buff> implements SoraConfig.KeyedTable<Integer, Buff> {
    public static final String NAME = "Buff";
    public static final SoraConfig.TableInfo INFO = new SoraConfig.TableInfo(
        NAME,
        "Buff",
        SoraConfig.TableShape.KEYED,
        new SoraConfig.KeyInfo("id", "Integer"),
        List.of(
        )
    );
    private final List<Integer> keys;
    private final java.util.Map<Integer, Buff> rows;

    private BuffTable(List<Integer> keys, java.util.Map<Integer, Buff> rows) {
        this.keys = List.copyOf(keys);
        this.rows = Map.copyOf(rows);
    }

    private static BuffTable fromRows(List<Buff> rows) {
        return new BuffTable(
            rows.stream().map(Buff::id).toList(),
            SoraConfig.decodeMapTable(rows, Buff::id)
        );
    }

    static BuffTable decode(SoraTableSource source) {
        return fromRows(source.decodeTable(NAME, Buff::decode, Buff::decode));
    }

    public java.util.Map<Integer, Buff> rows() {
        return rows;
    }
    @Override
    @SoraNullable
    public Buff get(Object key) {
        return rows.get(key);
    }

    @Override
    public List<Integer> orderedKeys() {
        return keys;
    }

    @Override
    public List<Buff> orderedRows() {
        return keys.stream().map(rows::get).toList();
    }

    @Override
    public Set<Map.Entry<Integer, Buff>> entrySet() {
        return rows.entrySet();
    }
    @Override
    public SoraConfig.TableInfo info() {
        return INFO;
    }

    @Override
    public int size() {
        return rows.size();
    }
}
