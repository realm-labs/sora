// This file was generated by Sora. Do not edit manually.

package com.sora.showcase;

import java.util.List;
import java.util.Map;
import java.util.Set;

public final class DialogueTable extends java.util.AbstractMap<Integer, Dialogue> implements SoraConfig.KeyedTable<Integer, Dialogue> {
    public static final String NAME = "Dialogue";
    public static final SoraConfig.TableInfo INFO = new SoraConfig.TableInfo(
        NAME,
        "Dialogue",
        SoraConfig.TableShape.KEYED,
        new SoraConfig.KeyInfo("id", "Integer"),
        List.of(
        )
    );
    private final List<Integer> keys;
    private final java.util.Map<Integer, Dialogue> rows;

    private DialogueTable(List<Integer> keys, java.util.Map<Integer, Dialogue> rows) {
        this.keys = List.copyOf(keys);
        this.rows = Map.copyOf(rows);
    }

    private static DialogueTable fromRows(List<Dialogue> rows) {
        return new DialogueTable(
            rows.stream().map(Dialogue::id).toList(),
            SoraConfig.decodeMapTable(rows, Dialogue::id)
        );
    }

    static DialogueTable decode(SoraTableSource source) {
        return fromRows(source.decodeTable(NAME, Dialogue::decode, Dialogue::decode));
    }

    public java.util.Map<Integer, Dialogue> rows() {
        return rows;
    }
    @Override
    @SoraNullable
    public Dialogue get(Object key) {
        return rows.get(key);
    }

    @Override
    public List<Integer> orderedKeys() {
        return keys;
    }

    @Override
    public List<Dialogue> orderedRows() {
        return keys.stream().map(rows::get).toList();
    }

    @Override
    public Set<Map.Entry<Integer, Dialogue>> entrySet() {
        return rows.entrySet();
    }
    @Override
    public SoraConfig.TableInfo info() {
        return INFO;
    }

    @Override
    public int size() {
        return rows.size();
    }
}
