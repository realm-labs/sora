// This file was generated by Sora. Do not edit manually.

package com.sora.showcase;

import java.util.List;
import java.util.Map;
import java.util.Set;

public final class GachaPoolTable extends java.util.AbstractMap<Integer, GachaPool> implements SoraConfig.KeyedTable<Integer, GachaPool> {
    public static final String NAME = "GachaPool";
    public static final SoraConfig.TableInfo INFO = new SoraConfig.TableInfo(
        NAME,
        "GachaPool",
        SoraConfig.TableShape.KEYED,
        new SoraConfig.KeyInfo("id", "Integer"),
        List.of(
        )
    );
    private final List<Integer> keys;
    private final java.util.Map<Integer, GachaPool> rows;

    private GachaPoolTable(List<Integer> keys, java.util.Map<Integer, GachaPool> rows) {
        this.keys = List.copyOf(keys);
        this.rows = Map.copyOf(rows);
    }

    private static GachaPoolTable fromRows(List<GachaPool> rows) {
        return new GachaPoolTable(
            rows.stream().map(GachaPool::id).toList(),
            SoraConfig.decodeMapTable(rows, GachaPool::id)
        );
    }

    static GachaPoolTable decode(SoraTableSource source) {
        return fromRows(source.decodeTable(NAME, GachaPool::decode, GachaPool::decode));
    }

    public java.util.Map<Integer, GachaPool> rows() {
        return rows;
    }
    @Override
    @SoraNullable
    public GachaPool get(Object key) {
        return rows.get(key);
    }

    @Override
    public List<Integer> orderedKeys() {
        return keys;
    }

    @Override
    public List<GachaPool> orderedRows() {
        return keys.stream().map(rows::get).toList();
    }

    @Override
    public Set<Map.Entry<Integer, GachaPool>> entrySet() {
        return rows.entrySet();
    }
    @Override
    public SoraConfig.TableInfo info() {
        return INFO;
    }

    @Override
    public int size() {
        return rows.size();
    }
}
