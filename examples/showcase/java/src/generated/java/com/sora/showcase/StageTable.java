// This file was generated by Sora. Do not edit manually.

package com.sora.showcase;

import java.util.List;
import java.util.Map;
import java.util.Set;

public final class StageTable extends java.util.AbstractMap<Integer, Stage> implements SoraConfig.KeyedTable<Integer, Stage> {
    public static final String NAME = "Stage";
    public static final SoraConfig.TableInfo INFO = new SoraConfig.TableInfo(
        NAME,
        "Stage",
        SoraConfig.TableShape.KEYED,
        new SoraConfig.KeyInfo("id", "Integer"),
        List.of(
        )
    );
    private final List<Integer> keys;
    private final java.util.Map<Integer, Stage> rows;

    private StageTable(List<Integer> keys, java.util.Map<Integer, Stage> rows) {
        this.keys = List.copyOf(keys);
        this.rows = Map.copyOf(rows);
    }

    private static StageTable fromRows(List<Stage> rows) {
        return new StageTable(
            rows.stream().map(Stage::id).toList(),
            SoraConfig.decodeMapTable(rows, Stage::id)
        );
    }

    static StageTable decode(SoraTableSource source) {
        return fromRows(source.decodeTable(NAME, Stage::decode, Stage::decode));
    }

    public java.util.Map<Integer, Stage> rows() {
        return rows;
    }
    @Override
    @SoraNullable
    public Stage get(Object key) {
        return rows.get(key);
    }

    @Override
    public List<Integer> orderedKeys() {
        return keys;
    }

    @Override
    public List<Stage> orderedRows() {
        return keys.stream().map(rows::get).toList();
    }

    @Override
    public Set<Map.Entry<Integer, Stage>> entrySet() {
        return rows.entrySet();
    }
    @Override
    public SoraConfig.TableInfo info() {
        return INFO;
    }

    @Override
    public int size() {
        return rows.size();
    }
}
