// This file was generated by Sora. Do not edit manually.

package com.sora.showcase;

import java.util.List;
import java.util.Map;
import java.util.Set;

public final class SkillTable extends java.util.AbstractMap<Integer, Skill> implements SoraConfig.KeyedTable<Integer, Skill> {
    public static final String NAME = "Skill";
    public static final SoraConfig.TableInfo INFO = new SoraConfig.TableInfo(
        NAME,
        "Skill",
        SoraConfig.TableShape.KEYED,
        new SoraConfig.KeyInfo("id", "Integer"),
        List.of(
        )
    );
    private final List<Integer> keys;
    private final java.util.Map<Integer, Skill> rows;

    private SkillTable(List<Integer> keys, java.util.Map<Integer, Skill> rows) {
        this.keys = List.copyOf(keys);
        this.rows = Map.copyOf(rows);
    }

    private static SkillTable fromRows(List<Skill> rows) {
        return new SkillTable(
            rows.stream().map(Skill::id).toList(),
            SoraConfig.decodeMapTable(rows, Skill::id)
        );
    }

    static SkillTable decode(SoraTableSource source) {
        return fromRows(source.decodeTable(NAME, Skill::decode, Skill::decode));
    }

    public java.util.Map<Integer, Skill> rows() {
        return rows;
    }
    @Override
    @SoraNullable
    public Skill get(Object key) {
        return rows.get(key);
    }

    @Override
    public List<Integer> orderedKeys() {
        return keys;
    }

    @Override
    public List<Skill> orderedRows() {
        return keys.stream().map(rows::get).toList();
    }

    @Override
    public Set<Map.Entry<Integer, Skill>> entrySet() {
        return rows.entrySet();
    }
    @Override
    public SoraConfig.TableInfo info() {
        return INFO;
    }

    @Override
    public int size() {
        return rows.size();
    }
}
