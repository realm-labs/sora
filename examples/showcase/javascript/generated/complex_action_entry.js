// This file was generated by Sora. Do not edit manually.
import { collectRewardActionTextKeys, decodeRewardAction, decodeRewardActionValue } from "./reward_action.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";

export function decodeComplexActionEntry(reader) {
    return {
        id: reader.readI32(),
        groupId: reader.readI32(),
        seq: reader.readI32(),
        value: decodeRewardAction(reader),
    };
}

export function decodeComplexActionEntryValue(value) {
    const object = value.asObject();
    return {
        id: object.get("id").asInt(),
        groupId: object.get("group_id").asInt(),
        seq: object.get("seq").asInt(),
        value: decodeRewardActionValue(object.get("value")),
    };
}

export function collectComplexActionEntryTextKeys(value, out) {
    collectRewardActionTextKeys(value.value, out);
}

export class ComplexActionEntryTable {
    static tableName = "ComplexActionEntry";
    static tableInfo = {
        name: ComplexActionEntryTable.tableName,
        rowType: "ComplexActionEntry",
        shape: "keyed",
        primaryKey: { name: "id", type: "number" },
        indexes: [
        ],
    };

    constructor(
        keys,
        rows,
    ) {
        this._rows = rows;
        this._keys = keys;
    }

    static decode(rows) {
        return new ComplexActionEntryTable(
            rows.map((row) => row.id),
            decodeMapTable(rows, (row) => row.id),
        );
    }

    info() {
        return ComplexActionEntryTable.tableInfo;
    }

    len() {
        return this._rows.size;
    }
    get(key) {
        return this._rows.get(key);
    }

    rows() {
        return this._rows;
    }

    keys() {
        return this._keys;
    }

    orderedRows() {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }
}
