// This file was generated by Sora. Do not edit manually.
import { collectResourceCostTextKeys, decodeResourceCost, decodeResourceCostValue } from "./resource_cost.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";

export function decodeGachaPool(reader) {
    return {
        id: reader.readI32(),
        name: reader.readString(),
        cost: decodeResourceCost(reader),
    };
}

export function decodeGachaPoolValue(value) {
    const object = value.asObject();
    return {
        id: object.get("id").asInt(),
        name: object.get("name").asString(),
        cost: decodeResourceCostValue(object.get("cost")),
    };
}

export function collectGachaPoolTextKeys(value, out) {
    collectResourceCostTextKeys(value.cost, out);
}

export class GachaPoolTable {
    static tableName = "GachaPool";
    static tableInfo = {
        name: GachaPoolTable.tableName,
        rowType: "GachaPool",
        shape: "keyed",
        primaryKey: { name: "id", type: "number" },
        indexes: [
        ],
    };

    constructor(
        keys,
        rows,
    ) {
        this._rows = rows;
        this._keys = keys;
    }

    static decode(rows) {
        return new GachaPoolTable(
            rows.map((row) => row.id),
            decodeMapTable(rows, (row) => row.id),
        );
    }

    info() {
        return GachaPoolTable.tableInfo;
    }

    get size() {
        return this._rows.size;
    }
    get(key) {
        return this._rows.get(key);
    }

    has(key) {
        return this._rows.has(key);
    }

    get rows() {
        return this._rows;
    }

    get orderedKeys() {
        return this._keys;
    }

    get orderedRows() {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }

    [Symbol.iterator]() {
        return this._rows.values();
    }
}
