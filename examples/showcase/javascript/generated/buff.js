// This file was generated by Sora. Do not edit manually.
import { decodeStatModifier, decodeStatModifierValue } from "./stat_modifier.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";

export function decodeBuff(reader) {
    return {
        id: reader.readI32(),
        name: reader.readString(),
        duration: reader.readF32(),
        modifiers: reader.readList(() => decodeStatModifier(reader)),
    };
}

export function decodeBuffValue(value) {
    const object = value.asObject();
    return {
        id: object.get("id").asInt(),
        name: object.get("name").asString(),
        duration: object.get("duration").asNumber(),
        modifiers: object.get("modifiers").asList((item) => decodeStatModifierValue(item)),
    };
}

export class BuffTable {
    static tableName = "Buff";

    constructor(
        keys,
        rows,
    ) {
        this._rows = rows;
        this._keys = keys;
    }

    static decode(rows) {
        return new BuffTable(
            rows.map((row) => row.id),
            decodeMapTable(rows, (row) => row.id),
        );
    }

    name() {
        return BuffTable.tableName;
    }

    mode() {
        return "map";
    }

    key() {
        return "id";
    }

    len() {
        return this._rows.size;
    }
    get(key) {
        return this._rows.get(key);
    }

    rows() {
        return this._rows;
    }

    keys() {
        return this._keys;
    }

    orderedRows() {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }
}
