// This file was generated by Sora. Do not edit manually.
import { collectEventConditionTextKeys, decodeEventCondition, decodeEventConditionValue } from "./event_condition.js";
import {
    decodeIndex,
    decodeMapTable,
    decodeUniqueIndex,
    requireSingletonTable,
} from "./sora_runtime.js";

export function decodeComplexConditionGroup(reader) {
    return {
        id: reader.readI32(),
        name: reader.readString(),
        conditions: reader.readList(() => decodeEventCondition(reader)),
    };
}

export function decodeComplexConditionGroupValue(value) {
    const object = value.asObject();
    return {
        id: object.get("id").asInt(),
        name: object.get("name").asString(),
        conditions: object.get("conditions").asList((item) => decodeEventConditionValue(item)),
    };
}

export function collectComplexConditionGroupTextKeys(value, out) {
    for (const item of value.conditions) { collectEventConditionTextKeys(item, out); }
}

export class ComplexConditionGroupTable {
    static tableName = "ComplexConditionGroup";
    static tableInfo = {
        name: ComplexConditionGroupTable.tableName,
        rowType: "ComplexConditionGroup",
        shape: "keyed",
        primaryKey: { name: "id", type: "number" },
        indexes: [
        ],
    };

    constructor(
        keys,
        rows,
    ) {
        this._rows = rows;
        this._keys = keys;
    }

    static decode(rows) {
        return new ComplexConditionGroupTable(
            rows.map((row) => row.id),
            decodeMapTable(rows, (row) => row.id),
        );
    }

    info() {
        return ComplexConditionGroupTable.tableInfo;
    }

    len() {
        return this._rows.size;
    }
    get(key) {
        return this._rows.get(key);
    }

    rows() {
        return this._rows;
    }

    keys() {
        return this._keys;
    }

    orderedRows() {
        return this._keys.flatMap((key) => {
            const row = this._rows.get(key);
            return row === undefined ? [] : [row];
        });
    }
}
