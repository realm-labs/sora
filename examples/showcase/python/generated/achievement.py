# This file was generated by Sora. Do not edit manually.

from __future__ import annotations

from dataclasses import dataclass
from collections.abc import Iterator, Mapping, Sequence
from typing import TYPE_CHECKING

from .sora_runtime import SoraReader, TextKey
from .sora_runtime import (
    SoraConfigTable,
    SoraIndexInfo,
    SoraKeyInfo,
    SoraTableInfo,
    decode_index,
    decode_map_table,
    decode_unique_index,
    require_singleton_table,
)


if TYPE_CHECKING:
    from .resource_cost import ResourceCost


@dataclass(frozen=True, slots=True)
class Achievement:
    id: int
    title_key: TextKey
    target_count: int
    reward: ResourceCost

    @staticmethod
    def decode(reader: SoraReader) -> Achievement:
        from .resource_cost import ResourceCost
        id = reader.read_i32()
        title_key = TextKey(reader.read_string())
        target_count = reader.read_i64()
        reward = ResourceCost.decode(reader)
        return Achievement(
            id=id,
            title_key=title_key,
            target_count=target_count,
            reward=reward,
        )

    def collect_text_keys(self, out: list[TextKey]) -> None:
        out.append(self.title_key)
        self.reward.collect_text_keys(out)


class AchievementTable(SoraConfigTable, Mapping[int, Achievement]):
    NAME = "Achievement"
    INFO = SoraTableInfo(
        name=NAME,
        row_type="Achievement",
        shape="keyed",
        primary_key=SoraKeyInfo("id", "int"),
        indexes=(
        ),
    )

    def __init__(
        self,
        keys: list[int],
        rows: dict[int, Achievement],
    ) -> None:
        self._keys = keys
        self._rows = rows

    @staticmethod
    def decode(rows: list[Achievement]) -> AchievementTable:
        return AchievementTable(
            [row.id for row in rows],
            decode_map_table(rows, lambda row: row.id),
        )

    def info(self) -> SoraTableInfo:
        return self.INFO

    def __len__(self) -> int:
        return len(self._rows)


    def __getitem__(self, key: int) -> Achievement:
        return self._rows[key]

    def __iter__(self) -> Iterator[int]:
        return iter(self._keys)

    @property
    def ordered_keys(self) -> tuple[int, ...]:
        return tuple(self._keys)

    @property
    def ordered_rows(self) -> tuple[Achievement, ...]:
        return tuple(self._rows[key] for key in self._keys)
