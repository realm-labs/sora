# This file was generated by Sora. Do not edit manually.

from __future__ import annotations

from dataclasses import dataclass
from collections.abc import Iterator, Mapping, Sequence
from typing import TYPE_CHECKING

from .sora_runtime import SoraReader, TextKey
from .sora_runtime import (
    SoraConfigTable,
    SoraIndexInfo,
    SoraKeyInfo,
    SoraTableInfo,
    decode_index,
    decode_map_table,
    decode_unique_index,
    require_singleton_table,
)


if TYPE_CHECKING:
    from .resource_cost import ResourceCost


@dataclass(frozen=True, slots=True)
class VipLevel:
    level: int
    cost: ResourceCost
    perks: list[str]

    @staticmethod
    def decode(reader: SoraReader) -> VipLevel:
        from .resource_cost import ResourceCost
        level = reader.read_i32()
        cost = ResourceCost.decode(reader)
        perks = reader.read_list(lambda: reader.read_string())
        return VipLevel(
            level=level,
            cost=cost,
            perks=perks,
        )

    def collect_text_keys(self, out: list[TextKey]) -> None:
        self.cost.collect_text_keys(out)


class VipLevelTable(SoraConfigTable, Mapping[int, VipLevel]):
    NAME = "VipLevel"
    INFO = SoraTableInfo(
        name=NAME,
        row_type="VipLevel",
        shape="keyed",
        primary_key=SoraKeyInfo("level", "int"),
        indexes=(
        ),
    )

    def __init__(
        self,
        keys: list[int],
        rows: dict[int, VipLevel],
    ) -> None:
        self._keys = keys
        self._rows = rows

    @staticmethod
    def decode(rows: list[VipLevel]) -> VipLevelTable:
        return VipLevelTable(
            [row.level for row in rows],
            decode_map_table(rows, lambda row: row.level),
        )

    def info(self) -> SoraTableInfo:
        return self.INFO

    def __len__(self) -> int:
        return len(self._rows)


    def __getitem__(self, key: int) -> VipLevel:
        return self._rows[key]

    def __iter__(self) -> Iterator[int]:
        return iter(self._keys)

    @property
    def ordered_keys(self) -> tuple[int, ...]:
        return tuple(self._keys)

    @property
    def ordered_rows(self) -> tuple[VipLevel, ...]:
        return tuple(self._rows[key] for key in self._keys)
