# This file was generated by Sora. Do not edit manually.

from __future__ import annotations

from dataclasses import dataclass
from collections.abc import Iterator, Mapping, Sequence
from typing import TYPE_CHECKING

from .sora_runtime import SoraReader, TextKey
from .sora_runtime import (
    SoraConfigTable,
    SoraIndexInfo,
    SoraKeyInfo,
    SoraTableInfo,
    decode_index,
    decode_map_table,
    decode_unique_index,
    require_singleton_table,
)


if TYPE_CHECKING:
    from .resource_cost import ResourceCost


@dataclass(frozen=True, slots=True)
class Dungeon:
    id: int
    name: str
    stage_ids: list[int]
    entry_cost: ResourceCost

    @staticmethod
    def decode(reader: SoraReader) -> Dungeon:
        from .resource_cost import ResourceCost
        id = reader.read_i32()
        name = reader.read_string()
        stage_ids = reader.read_list(lambda: reader.read_i32())
        entry_cost = ResourceCost.decode(reader)
        return Dungeon(
            id=id,
            name=name,
            stage_ids=stage_ids,
            entry_cost=entry_cost,
        )

    def collect_text_keys(self, out: list[TextKey]) -> None:
        self.entry_cost.collect_text_keys(out)


class DungeonTable(SoraConfigTable, Mapping[int, Dungeon]):
    NAME = "Dungeon"
    INFO = SoraTableInfo(
        name=NAME,
        row_type="Dungeon",
        shape="keyed",
        primary_key=SoraKeyInfo("id", "int"),
        indexes=(
        ),
    )

    def __init__(
        self,
        keys: list[int],
        rows: dict[int, Dungeon],
    ) -> None:
        self._keys = keys
        self._rows = rows

    @staticmethod
    def decode(rows: list[Dungeon]) -> DungeonTable:
        return DungeonTable(
            [row.id for row in rows],
            decode_map_table(rows, lambda row: row.id),
        )

    def info(self) -> SoraTableInfo:
        return self.INFO

    def __len__(self) -> int:
        return len(self._rows)


    def __getitem__(self, key: int) -> Dungeon:
        return self._rows[key]

    def __iter__(self) -> Iterator[int]:
        return iter(self._keys)

    @property
    def ordered_keys(self) -> tuple[int, ...]:
        return tuple(self._keys)

    @property
    def ordered_rows(self) -> tuple[Dungeon, ...]:
        return tuple(self._rows[key] for key in self._keys)
