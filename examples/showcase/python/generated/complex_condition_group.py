# This file was generated by Sora. Do not edit manually.

from __future__ import annotations

from dataclasses import dataclass
from collections.abc import Iterator, Mapping, Sequence
from typing import TYPE_CHECKING

from .sora_runtime import SoraReader, TextKey
from .sora_runtime import (
    SoraConfigTable,
    SoraIndexInfo,
    SoraKeyInfo,
    SoraTableInfo,
    decode_index,
    decode_map_table,
    decode_unique_index,
    require_singleton_table,
)


if TYPE_CHECKING:
    from .event_condition import EventCondition


@dataclass(frozen=True, slots=True)
class ComplexConditionGroup:
    id: int
    name: str
    # A derived list of union values; each child row is edited without JSON
    conditions: list[EventCondition]

    @staticmethod
    def decode(reader: SoraReader) -> ComplexConditionGroup:
        from .event_condition import EventCondition
        id = reader.read_i32()
        name = reader.read_string()
        conditions = reader.read_list(lambda: EventCondition.decode(reader))
        return ComplexConditionGroup(
            id=id,
            name=name,
            conditions=conditions,
        )

    def collect_text_keys(self, out: list[TextKey]) -> None:
        for item in self.conditions:
            item.collect_text_keys(out)


class ComplexConditionGroupTable(SoraConfigTable, Mapping[int, ComplexConditionGroup]):
    NAME = "ComplexConditionGroup"
    INFO = SoraTableInfo(
        name=NAME,
        row_type="ComplexConditionGroup",
        shape="keyed",
        primary_key=SoraKeyInfo("id", "int"),
        indexes=(
        ),
    )

    def __init__(
        self,
        keys: list[int],
        rows: dict[int, ComplexConditionGroup],
    ) -> None:
        self._keys = keys
        self._rows = rows

    @staticmethod
    def decode(rows: list[ComplexConditionGroup]) -> ComplexConditionGroupTable:
        return ComplexConditionGroupTable(
            [row.id for row in rows],
            decode_map_table(rows, lambda row: row.id),
        )

    def info(self) -> SoraTableInfo:
        return self.INFO

    def __len__(self) -> int:
        return len(self._rows)


    def __getitem__(self, key: int) -> ComplexConditionGroup:
        return self._rows[key]

    def __iter__(self) -> Iterator[int]:
        return iter(self._keys)

    @property
    def ordered_keys(self) -> tuple[int, ...]:
        return tuple(self._keys)

    @property
    def ordered_rows(self) -> tuple[ComplexConditionGroup, ...]:
        return tuple(self._rows[key] for key in self._keys)
