# This file was generated by Sora. Do not edit manually.

from __future__ import annotations

from dataclasses import dataclass
from typing import TYPE_CHECKING

from .sora_runtime import SoraReader, TextKey
from .sora_runtime import (
    SoraConfigTable,
    SoraIndexInfo,
    SoraKeyInfo,
    SoraTableInfo,
    decode_index,
    decode_map_table,
    decode_unique_index,
    require_singleton_table,
)


if TYPE_CHECKING:
    from .complex_budget import ComplexBudget
    from .event_condition import EventCondition
    from .reward_action import RewardAction


@dataclass(frozen=True, slots=True)
class ComplexRule:
    id: int
    name: str
    # Single union value derived from a tagged_columns child row
    root_condition: EventCondition
    root_action_group: int
    # Non-JSON list<union<RewardAction>> assembled from child rows
    actions: list[RewardAction]
    # Nested tuple, tuple_list, split, and map parsers in one cell
    budget: ComplexBudget

    @staticmethod
    def decode(reader: SoraReader) -> ComplexRule:
        from .complex_budget import ComplexBudget
        from .event_condition import EventCondition
        from .reward_action import RewardAction
        id = reader.read_i32()
        name = reader.read_string()
        root_condition = EventCondition.decode(reader)
        root_action_group = reader.read_i32()
        actions = reader.read_list(lambda: RewardAction.decode(reader))
        budget = ComplexBudget.decode(reader)
        return ComplexRule(
            id=id,
            name=name,
            root_condition=root_condition,
            root_action_group=root_action_group,
            actions=actions,
            budget=budget,
        )

    def collect_text_keys(self, out: list[TextKey]) -> None:
        self.root_condition.collect_text_keys(out)
        for item in self.actions:
            item.collect_text_keys(out)
        self.budget.collect_text_keys(out)


class ComplexRuleTable(SoraConfigTable):
    NAME = "ComplexRule"
    INFO = SoraTableInfo(
        name=NAME,
        row_type="ComplexRule",
        shape="keyed",
        primary_key=SoraKeyInfo("id", "int"),
        indexes=(
        ),
    )

    def __init__(
        self,
        keys: list[int],
        rows: dict[int, ComplexRule],
    ) -> None:
        self._keys = keys
        self._rows = rows

    @staticmethod
    def decode(rows: list[ComplexRule]) -> ComplexRuleTable:
        return ComplexRuleTable(
            [row.id for row in rows],
            decode_map_table(rows, lambda row: row.id),
        )

    def info(self) -> SoraTableInfo:
        return self.INFO

    def len(self) -> int:
        return len(self._rows)


    def get(self, key: int) -> ComplexRule | None:
        return self._rows.get(key)

    def rows(self) -> dict[int, ComplexRule]:
        return self._rows

    def keys(self) -> list[int]:
        return self._keys

    def ordered_rows(self) -> list[ComplexRule]:
        return [self._rows[key] for key in self._keys if key in self._rows]
