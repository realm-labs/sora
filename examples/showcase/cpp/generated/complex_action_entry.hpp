// This file was generated by Sora. Do not edit manually.

#pragma once

#include "sora_runtime.hpp"
#include "reward_action.hpp"

#include <unordered_map>
#include <string>
#include <vector>

namespace sora::showcase {

struct ComplexActionEntry {
    std::int32_t id;
    std::int32_t group_id;
    std::int32_t seq;
    RewardAction value;

    static ComplexActionEntry decode(SoraReader& reader) {
        return ComplexActionEntry{
            reader.read_i32(),
            reader.read_i32(),
            reader.read_i32(),
            RewardAction::decode(reader),
        };
    }
};

class ComplexActionEntryTable final : public SoraTable {
public:
    static constexpr const char* NAME = "ComplexActionEntry";
    static const SoraTableInfo& table_info() {
        static const SoraTableInfo info = {
            NAME,
            "ComplexActionEntry",
            "keyed",
            "id"
        };
        return info;
    }

    ComplexActionEntryTable() {}
    ComplexActionEntryTable(const ComplexActionEntryTable&) = delete;
    ComplexActionEntryTable& operator=(const ComplexActionEntryTable&) = delete;
    ComplexActionEntryTable(ComplexActionEntryTable&&) = default;
    ComplexActionEntryTable& operator=(ComplexActionEntryTable&&) = default;

    static ComplexActionEntryTable decode(const SoraBundle& bundle) {
        std::vector<ComplexActionEntry> rows = bundle.decode_table<ComplexActionEntry>(NAME);
        ComplexActionEntryTable table;
        for (std::size_t index = 0; index < rows.size(); ++index) {
            const ComplexActionEntry& row = rows[index];
            table.keys_.push_back(row.id);
            table.rows_.emplace(row.id, row);
        }
        table.build_indexes();
        return table;
    }

    const SoraTableInfo& info() const override { return table_info(); }
    std::size_t size() const override {
        return rows_.size();
    }
    const std::vector<std::int32_t>& keys() const {
        return keys_;
    }

    const ComplexActionEntry* get(const std::int32_t& key) const {
        typename std::unordered_map<std::int32_t, ComplexActionEntry>::const_iterator it = rows_.find(key);
        if (it == rows_.end()) {
            return nullptr;
        }
        return &it->second;
    }

    const std::unordered_map<std::int32_t, ComplexActionEntry>& rows() const {
        return rows_;
    }

    std::vector<const ComplexActionEntry*> ordered_rows() const {
        std::vector<const ComplexActionEntry*> rows;
        rows.reserve(keys_.size());
        for (typename std::vector<std::int32_t>::const_iterator key = keys_.begin();
             key != keys_.end();
             ++key) {
            typename std::unordered_map<std::int32_t, ComplexActionEntry>::const_iterator it = rows_.find(*key);
            if (it != rows_.end()) {
                rows.push_back(&it->second);
            }
        }
        return rows;
    }

private:
    void build_indexes() {
    }
    std::vector<std::int32_t> keys_;
    std::unordered_map<std::int32_t, ComplexActionEntry> rows_;
};

} // namespace sora::showcase
