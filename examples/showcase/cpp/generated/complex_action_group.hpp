// This file was generated by Sora. Do not edit manually.

#pragma once

#include "sora_runtime.hpp"
#include "reward_action.hpp"

#include <unordered_map>
#include <string>
#include <vector>

namespace sora::showcase {

struct ComplexActionGroup {
    std::int32_t id;
    std::string name;
    std::vector<RewardAction> actions;

    static ComplexActionGroup decode(SoraReader& reader) {
        return ComplexActionGroup{
            reader.read_i32(),
            reader.read_string(),
            reader.read_vector<RewardAction>(),
        };
    }
};

class ComplexActionGroupTable final : public SoraTable {
public:
    static constexpr const char* NAME = "ComplexActionGroup";
    static const SoraTableInfo& table_info() {
        static const SoraTableInfo info = {
            NAME,
            "ComplexActionGroup",
            "keyed",
            "id"
        };
        return info;
    }

    ComplexActionGroupTable() {}
    ComplexActionGroupTable(const ComplexActionGroupTable&) = delete;
    ComplexActionGroupTable& operator=(const ComplexActionGroupTable&) = delete;
    ComplexActionGroupTable(ComplexActionGroupTable&&) = default;
    ComplexActionGroupTable& operator=(ComplexActionGroupTable&&) = default;

    static ComplexActionGroupTable decode(const SoraBundle& bundle) {
        std::vector<ComplexActionGroup> rows = bundle.decode_table<ComplexActionGroup>(NAME);
        ComplexActionGroupTable table;
        for (std::size_t index = 0; index < rows.size(); ++index) {
            const ComplexActionGroup& row = rows[index];
            table.keys_.push_back(row.id);
            table.rows_.emplace(row.id, row);
        }
        table.build_indexes();
        return table;
    }

    const SoraTableInfo& info() const override { return table_info(); }
    std::size_t size() const override {
        return rows_.size();
    }
    const std::vector<std::int32_t>& keys() const {
        return keys_;
    }

    const ComplexActionGroup* get(const std::int32_t& key) const {
        typename std::unordered_map<std::int32_t, ComplexActionGroup>::const_iterator it = rows_.find(key);
        if (it == rows_.end()) {
            return nullptr;
        }
        return &it->second;
    }

    const std::unordered_map<std::int32_t, ComplexActionGroup>& rows() const {
        return rows_;
    }

    std::vector<const ComplexActionGroup*> ordered_rows() const {
        std::vector<const ComplexActionGroup*> rows;
        rows.reserve(keys_.size());
        for (typename std::vector<std::int32_t>::const_iterator key = keys_.begin();
             key != keys_.end();
             ++key) {
            typename std::unordered_map<std::int32_t, ComplexActionGroup>::const_iterator it = rows_.find(*key);
            if (it != rows_.end()) {
                rows.push_back(&it->second);
            }
        }
        return rows;
    }

private:
    void build_indexes() {
    }
    std::vector<std::int32_t> keys_;
    std::unordered_map<std::int32_t, ComplexActionGroup> rows_;
};

} // namespace sora::showcase
