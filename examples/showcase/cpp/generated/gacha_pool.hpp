// This file was generated by Sora. Do not edit manually.

#pragma once

#include "sora_runtime.hpp"
#include "resource_cost.hpp"

#include <unordered_map>
#include <string>
#include <vector>

namespace sora::showcase {

struct GachaPool {
    std::int32_t id;
    std::string name;
    ResourceCost cost;

    static GachaPool decode(SoraReader& reader) {
        return GachaPool{
            reader.read_i32(),
            reader.read_string(),
            ResourceCost::decode(reader),
        };
    }
};

class GachaPoolTable final : public SoraTable {
public:
    static constexpr const char* NAME = "GachaPool";
    static const SoraTableInfo& table_info() {
        static const SoraTableInfo info = {
            NAME,
            "GachaPool",
            "keyed",
            "id"
        };
        return info;
    }

    GachaPoolTable() = default;
    GachaPoolTable(const GachaPoolTable&) = delete;
    GachaPoolTable& operator=(const GachaPoolTable&) = delete;
    GachaPoolTable(GachaPoolTable&&) = default;
    GachaPoolTable& operator=(GachaPoolTable&&) = default;

    static GachaPoolTable decode(const SoraBundle& bundle) {
        std::vector<GachaPool> rows = bundle.decode_table<GachaPool>(NAME);
        GachaPoolTable table;
        for (const auto& row : rows) {
            table.keys_.push_back(row.id);
            table.rows_.emplace(row.id, row);
        }
        table.build_indexes();
        return table;
    }

    const SoraTableInfo& info() const override { return table_info(); }
    std::size_t size() const override {
        return rows_.size();
    }
    const std::vector<std::int32_t>& keys() const {
        return keys_;
    }

    const GachaPool* get(const std::int32_t& key) const {
        auto it = rows_.find(key);
        if (it == rows_.end()) {
            return nullptr;
        }
        return &it->second;
    }

    const std::unordered_map<std::int32_t, GachaPool>& rows() const {
        return rows_;
    }

    std::vector<const GachaPool*> ordered_rows() const {
        std::vector<const GachaPool*> rows;
        rows.reserve(keys_.size());
        for (const auto& key : keys_) {
            auto it = rows_.find(key);
            if (it != rows_.end()) {
                rows.push_back(&it->second);
            }
        }
        return rows;
    }

private:
    void build_indexes() {
    }
    std::vector<std::int32_t> keys_;
    std::unordered_map<std::int32_t, GachaPool> rows_;
};

} // namespace sora::showcase
