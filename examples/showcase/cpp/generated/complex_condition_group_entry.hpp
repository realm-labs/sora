// This file was generated by Sora. Do not edit manually.

#pragma once

#include "sora_runtime.hpp"
#include "event_condition.hpp"

#include <unordered_map>
#include <string>
#include <vector>

namespace sora::showcase {

struct ComplexConditionGroupEntry {
    std::int32_t id;
    std::int32_t group_id;
    std::int32_t seq;
    EventCondition value;

    static ComplexConditionGroupEntry decode(SoraReader& reader) {
        return ComplexConditionGroupEntry{
            reader.read_i32(),
            reader.read_i32(),
            reader.read_i32(),
            EventCondition::decode(reader),
        };
    }
};

class ComplexConditionGroupEntryTable final : public SoraTable {
public:
    static constexpr const char* NAME = "ComplexConditionGroupEntry";
    static const SoraTableInfo& table_info() {
        static const SoraTableInfo info = {
            NAME,
            "ComplexConditionGroupEntry",
            "keyed",
            "id"
        };
        return info;
    }

    ComplexConditionGroupEntryTable() {}
    ComplexConditionGroupEntryTable(const ComplexConditionGroupEntryTable&) = delete;
    ComplexConditionGroupEntryTable& operator=(const ComplexConditionGroupEntryTable&) = delete;
    ComplexConditionGroupEntryTable(ComplexConditionGroupEntryTable&&) = default;
    ComplexConditionGroupEntryTable& operator=(ComplexConditionGroupEntryTable&&) = default;

    static ComplexConditionGroupEntryTable decode(const SoraBundle& bundle) {
        std::vector<ComplexConditionGroupEntry> rows = bundle.decode_table<ComplexConditionGroupEntry>(NAME);
        ComplexConditionGroupEntryTable table;
        for (std::size_t index = 0; index < rows.size(); ++index) {
            const ComplexConditionGroupEntry& row = rows[index];
            table.keys_.push_back(row.id);
            table.rows_.emplace(row.id, row);
        }
        table.build_indexes();
        return table;
    }

    const SoraTableInfo& info() const override { return table_info(); }
    std::size_t size() const override {
        return rows_.size();
    }
    const std::vector<std::int32_t>& keys() const {
        return keys_;
    }

    const ComplexConditionGroupEntry* get(const std::int32_t& key) const {
        typename std::unordered_map<std::int32_t, ComplexConditionGroupEntry>::const_iterator it = rows_.find(key);
        if (it == rows_.end()) {
            return nullptr;
        }
        return &it->second;
    }

    const std::unordered_map<std::int32_t, ComplexConditionGroupEntry>& rows() const {
        return rows_;
    }

    std::vector<const ComplexConditionGroupEntry*> ordered_rows() const {
        std::vector<const ComplexConditionGroupEntry*> rows;
        rows.reserve(keys_.size());
        for (typename std::vector<std::int32_t>::const_iterator key = keys_.begin();
             key != keys_.end();
             ++key) {
            typename std::unordered_map<std::int32_t, ComplexConditionGroupEntry>::const_iterator it = rows_.find(*key);
            if (it != rows_.end()) {
                rows.push_back(&it->second);
            }
        }
        return rows;
    }

private:
    void build_indexes() {
    }
    std::vector<std::int32_t> keys_;
    std::unordered_map<std::int32_t, ComplexConditionGroupEntry> rows_;
};

} // namespace sora::showcase
