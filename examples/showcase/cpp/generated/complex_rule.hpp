// This file was generated by Sora. Do not edit manually.

#pragma once

#include "sora_runtime.hpp"
#include "complex_budget.hpp"
#include "event_condition.hpp"
#include "reward_action.hpp"

#include <unordered_map>
#include <string>
#include <vector>

namespace sora::showcase {

struct ComplexRule {
    std::int32_t id;
    std::string name;
    // Single union value derived from a tagged_columns child row
    EventCondition root_condition;
    std::int32_t root_action_group;
    // Non-JSON list<union<RewardAction>> assembled from child rows
    std::vector<RewardAction> actions;
    // Nested tuple, tuple_list, split, and map parsers in one cell
    ComplexBudget budget;

    static ComplexRule decode(SoraReader& reader) {
        return ComplexRule{
            reader.read_i32(),
            reader.read_string(),
            EventCondition::decode(reader),
            reader.read_i32(),
            reader.read_vector<RewardAction>(),
            ComplexBudget::decode(reader),
        };
    }
};

class ComplexRuleTable final : public SoraTable {
public:
    static constexpr const char* NAME = "ComplexRule";
    static const SoraTableInfo& table_info() {
        static const SoraTableInfo info = {
            NAME,
            "ComplexRule",
            "keyed",
            "id"
        };
        return info;
    }

    ComplexRuleTable() {}
    ComplexRuleTable(const ComplexRuleTable&) = delete;
    ComplexRuleTable& operator=(const ComplexRuleTable&) = delete;
    ComplexRuleTable(ComplexRuleTable&&) = default;
    ComplexRuleTable& operator=(ComplexRuleTable&&) = default;

    static ComplexRuleTable decode(const SoraBundle& bundle) {
        std::vector<ComplexRule> rows = bundle.decode_table<ComplexRule>(NAME);
        ComplexRuleTable table;
        for (std::size_t index = 0; index < rows.size(); ++index) {
            const ComplexRule& row = rows[index];
            table.keys_.push_back(row.id);
            table.rows_.emplace(row.id, row);
        }
        table.build_indexes();
        return table;
    }

    const SoraTableInfo& info() const override { return table_info(); }
    std::size_t size() const override {
        return rows_.size();
    }
    const std::vector<std::int32_t>& keys() const {
        return keys_;
    }

    const ComplexRule* get(const std::int32_t& key) const {
        typename std::unordered_map<std::int32_t, ComplexRule>::const_iterator it = rows_.find(key);
        if (it == rows_.end()) {
            return nullptr;
        }
        return &it->second;
    }

    const std::unordered_map<std::int32_t, ComplexRule>& rows() const {
        return rows_;
    }

    std::vector<const ComplexRule*> ordered_rows() const {
        std::vector<const ComplexRule*> rows;
        rows.reserve(keys_.size());
        for (typename std::vector<std::int32_t>::const_iterator key = keys_.begin();
             key != keys_.end();
             ++key) {
            typename std::unordered_map<std::int32_t, ComplexRule>::const_iterator it = rows_.find(*key);
            if (it != rows_.end()) {
                rows.push_back(&it->second);
            }
        }
        return rows;
    }

private:
    void build_indexes() {
    }
    std::vector<std::int32_t> keys_;
    std::unordered_map<std::int32_t, ComplexRule> rows_;
};

} // namespace sora::showcase
