// This file was generated by Sora. Do not edit manually.

import 'runtime.dart';
import 'reward_action.dart';

final class ComplexActionGroup {
  final int id;
  final String name;
  final List<RewardAction> actions;

  const ComplexActionGroup({
    required this.id,
    required this.name,
    required this.actions,
  });

  static ComplexActionGroup decode(SoraValue value) {
    final obj = value.asObject();
    return ComplexActionGroup(
      id: obj.get("id").asInt(),
      name: obj.get("name").asString(),
      actions: obj.get("actions").asList((item) => RewardAction.decode(item)),
    );
  }

  void collectTextKeys(List<TextKey> out) {
    for (final item in this.actions) {
      item.collectTextKeys(out);
    }
  }
}

final class ComplexActionGroupTable extends Iterable<ComplexActionGroup> implements SoraKeyedTable<int, ComplexActionGroup> {
  static const tableName = 'ComplexActionGroup';
  static const tableInfo = SoraTableInfo(
    name: tableName,
    rowType: 'ComplexActionGroup',
    shape: SoraTableShape.keyed,
    primaryKey: SoraKeyInfo('id', 'int'),
    indexes: [
    ],
  );
  final List<int> _keys;
  final Map<int, ComplexActionGroup> _rows;

  const ComplexActionGroupTable(
    this._keys,
    this._rows,
  );

  static ComplexActionGroupTable decode(List<ComplexActionGroup> rows) {
    return ComplexActionGroupTable(
      rows.map((row) => row.id).toList(growable: false),
      decodeMapTable(rows, (row) => row.id),
    );
  }

  @override
  SoraTableInfo get info => tableInfo;

  @override
  int get length => _rows.length;

  @override
  Iterator<ComplexActionGroup> get iterator => _rows.values.iterator;
  ComplexActionGroup? operator [](int key) => _rows[key];

  ComplexActionGroup? get(int key) => _rows[key];

  Map<int, ComplexActionGroup> get rows => _rows;

  List<int> get keys => _keys;

  List<ComplexActionGroup> get orderedRows => [
        for (final key in _keys)
          if (_rows[key] != null) _rows[key]!,
      ];
}
