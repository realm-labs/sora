// This file was generated by Sora. Do not edit manually.

package com.sora.showcase

final case class ComplexConditionGroup(
  id: Int,
  name: String,
  /** A derived list of union values; each child row is edited without JSON */
  conditions: Vector[EventCondition]
) {
  def collectTextKeys(out: scala.collection.mutable.ArrayBuffer[TextKey]): Unit = {
    this.conditions.foreach { item =>
      EventCondition.collectTextKeys(item, out)
    }
  }
}

object ComplexConditionGroup {
  def decode(reader: SoraReader): ComplexConditionGroup =
    ComplexConditionGroup(
      id = reader.readI32(),
      name = reader.readString(),
      conditions = reader.readList(EventCondition.decode(reader))
    )

  def decode(value: SoraValue): ComplexConditionGroup = {
    val obj = value.asObject
    ComplexConditionGroup(
      id = obj.get("id").asInt,
      name = obj.get("name").asString,
      conditions = obj.get("conditions").asList(item => EventCondition.decode(item))
    )
  }
}

final class ComplexConditionGroupTable private (
  val keys: Vector[Int],
  val rows: Map[Int, ComplexConditionGroup]
) extends SoraTable {
  def get(key: Int): Option[ComplexConditionGroup] = rows.get(key)

  def values: Iterable[ComplexConditionGroup] = rows.values

  def orderedValues: Vector[ComplexConditionGroup] = keys.flatMap(rows.get)
  override val info: SoraTableInfo = ComplexConditionGroupTable.Info
  override def size: Int = rows.size
}

object ComplexConditionGroupTable {
  val Name: String = "ComplexConditionGroup"
  val Info: SoraTableInfo = SoraTableInfo(
    name = Name,
    rowType = "ComplexConditionGroup",
    shape = SoraTableShape.Keyed,
    primaryKey = Some(SoraKeyInfo("id", "Int")),
    indexes = Vector(
    )
  )

  def decode(source: SoraTableSource): ComplexConditionGroupTable =
    fromRows(source.decodeTable(Name, (reader: SoraReader) => ComplexConditionGroup.decode(reader), (value: SoraValue) => ComplexConditionGroup.decode(value)))

  private def fromRows(rows: Vector[ComplexConditionGroup]): ComplexConditionGroupTable =
    new ComplexConditionGroupTable(
      rows.map(row => row.id),
      rows.map(row => row.id -> row).toMap
    )
}
