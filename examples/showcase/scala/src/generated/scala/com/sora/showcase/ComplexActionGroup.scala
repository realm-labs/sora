// This file was generated by Sora. Do not edit manually.

package com.sora.showcase

final case class ComplexActionGroup(
  id: Int,
  name: String,
  actions: Vector[RewardAction]
) {
  def collectTextKeys(out: scala.collection.mutable.ArrayBuffer[TextKey]): Unit = {
    this.actions.foreach { item =>
      RewardAction.collectTextKeys(item, out)
    }
  }
}

object ComplexActionGroup {
  def decode(reader: SoraReader): ComplexActionGroup =
    ComplexActionGroup(
      id = reader.readI32(),
      name = reader.readString(),
      actions = reader.readList(RewardAction.decode(reader))
    )

  def decode(value: SoraValue): ComplexActionGroup = {
    val obj = value.asObject
    ComplexActionGroup(
      id = obj.get("id").asInt,
      name = obj.get("name").asString,
      actions = obj.get("actions").asList(item => RewardAction.decode(item))
    )
  }
}

final class ComplexActionGroupTable private (
  val keys: Vector[Int],
  val rows: Map[Int, ComplexActionGroup]
) extends SoraTable {
  def get(key: Int): Option[ComplexActionGroup] = rows.get(key)

  def values: Iterable[ComplexActionGroup] = rows.values

  def orderedValues: Vector[ComplexActionGroup] = keys.flatMap(rows.get)
  override val info: SoraTableInfo = ComplexActionGroupTable.Info
  override def size: Int = rows.size
}

object ComplexActionGroupTable {
  val Name: String = "ComplexActionGroup"
  val Info: SoraTableInfo = SoraTableInfo(
    name = Name,
    rowType = "ComplexActionGroup",
    shape = SoraTableShape.Keyed,
    primaryKey = Some(SoraKeyInfo("id", "Int")),
    indexes = Vector(
    )
  )

  def decode(source: SoraTableSource): ComplexActionGroupTable =
    fromRows(source.decodeTable(Name, (reader: SoraReader) => ComplexActionGroup.decode(reader), (value: SoraValue) => ComplexActionGroup.decode(value)))

  private def fromRows(rows: Vector[ComplexActionGroup]): ComplexActionGroupTable =
    new ComplexActionGroupTable(
      rows.map(row => row.id),
      rows.map(row => row.id -> row).toMap
    )
}
