// This file was generated by Sora. Do not edit manually.

package com.sora.showcase

final case class ComplexRule(
  id: Int,
  name: String,
  /** Single union value derived from a tagged_columns child row */
  rootCondition: EventCondition,
  rootActionGroup: Int,
  /** Non-JSON list<union<RewardAction>> assembled from child rows */
  actions: Vector[RewardAction],
  /** Nested tuple, tuple_list, split, and map parsers in one cell */
  budget: ComplexBudget
) {
  def collectTextKeys(out: scala.collection.mutable.ArrayBuffer[TextKey]): Unit = {
    EventCondition.collectTextKeys(this.rootCondition, out)
    this.actions.foreach { item =>
      RewardAction.collectTextKeys(item, out)
    }
    this.budget.collectTextKeys(out)
  }
}

object ComplexRule {
  def decode(reader: SoraReader): ComplexRule =
    ComplexRule(
      id = reader.readI32(),
      name = reader.readString(),
      rootCondition = EventCondition.decode(reader),
      rootActionGroup = reader.readI32(),
      actions = reader.readList(RewardAction.decode(reader)),
      budget = ComplexBudget.decode(reader)
    )

  def decode(value: SoraValue): ComplexRule = {
    val obj = value.asObject
    ComplexRule(
      id = obj.get("id").asInt,
      name = obj.get("name").asString,
      rootCondition = EventCondition.decode(obj.get("root_condition")),
      rootActionGroup = obj.get("root_action_group").asInt,
      actions = obj.get("actions").asList(item => RewardAction.decode(item)),
      budget = ComplexBudget.decode(obj.get("budget"))
    )
  }
}

final class ComplexRuleTable private (
  val keys: Vector[Int],
  val rows: Map[Int, ComplexRule]
) extends SoraTable {
  def get(key: Int): Option[ComplexRule] = rows.get(key)

  def values: Iterable[ComplexRule] = rows.values

  def orderedValues: Vector[ComplexRule] = keys.flatMap(rows.get)
  override val info: SoraTableInfo = ComplexRuleTable.Info
  override def size: Int = rows.size
}

object ComplexRuleTable {
  val Name: String = "ComplexRule"
  val Info: SoraTableInfo = SoraTableInfo(
    name = Name,
    rowType = "ComplexRule",
    shape = SoraTableShape.Keyed,
    primaryKey = Some(SoraKeyInfo("id", "Int")),
    indexes = Vector(
    )
  )

  def decode(source: SoraTableSource): ComplexRuleTable =
    fromRows(source.decodeTable(Name, (reader: SoraReader) => ComplexRule.decode(reader), (value: SoraValue) => ComplexRule.decode(value)))

  private def fromRows(rows: Vector[ComplexRule]): ComplexRuleTable =
    new ComplexRuleTable(
      rows.map(row => row.id),
      rows.map(row => row.id -> row).toMap
    )
}
