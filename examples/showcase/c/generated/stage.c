/* This file was generated by Sora. Do not edit manually. */

#include "stage.h"
#include <stdlib.h>

sora_result sora_showcase_stage_decode(sora_reader* reader, sora_showcase_stage* out) {
    *out = (sora_showcase_stage){0};
    {
        sora_result result = sora_reader_read_i32(reader, &out->id);
        if (result.code != SORA_OK) {
            sora_showcase_stage_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_string(reader, &out->name);
        if (result.code != SORA_OK) {
            sora_showcase_stage_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_showcase_i32_array_decode(reader, &out->monster_ids);
        if (result.code != SORA_OK) {
            sora_showcase_stage_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_i32(reader, &out->recommended_power);
        if (result.code != SORA_OK) {
            sora_showcase_stage_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_showcase_reward_array_decode(reader, &out->first_clear_rewards);
        if (result.code != SORA_OK) {
            sora_showcase_stage_free(out);
            return result;
        }
    }
    return sora_ok();
}

void sora_showcase_stage_free(sora_showcase_stage* value) {
    if (value == NULL) {
        return;
    }
    sora_string_free(&value->name);
    sora_showcase_i32_array_free(&value->monster_ids);
    sora_showcase_reward_array_free(&value->first_clear_rewards);
    *value = (sora_showcase_stage){0};
}

static const char* sora_showcase_stage_table_name = "Stage";

struct sora_showcase_stage_table {
    sora_showcase_stage* rows;
    size_t len;
};

static void sora_showcase_stage_table_free_rows(sora_showcase_stage* rows, size_t len) {
    if (rows == NULL) {
        return;
    }
    for (size_t index = 0; index < len; ++index) {
        sora_showcase_stage_free(&rows[index]);
    }
    free(rows);
}

sora_result sora_showcase_stage_table_load(
    const sora_bundle* bundle,
    sora_showcase_stage_table** out
) {
    sora_showcase_stage_table* table = (sora_showcase_stage_table*)calloc(1, sizeof(sora_showcase_stage_table));
    if (table == NULL) {
        return sora_error(SORA_ERROR_OUT_OF_MEMORY, "failed to allocate table");
    }
    sora_result result = sora_bundle_decode_table(
        bundle,
        sora_showcase_stage_table_name,
        sizeof(sora_showcase_stage),
        (sora_decode_row_fn)sora_showcase_stage_decode,
        (sora_free_row_fn)sora_showcase_stage_free,
        (void**)&table->rows,
        &table->len
    );
    if (result.code != SORA_OK) {
        sora_showcase_stage_table_free(table);
        return result;
    }
    *out = table;
    return sora_ok();
}

void sora_showcase_stage_table_free(sora_showcase_stage_table* table) {
    if (table == NULL) {
        return;
    }
    sora_showcase_stage_table_free_rows(table->rows, table->len);
    free(table);
}

const sora_showcase_stage* sora_showcase_stage_table_rows(
    const sora_showcase_stage_table* table,
    size_t* out_len
) {
    if (out_len != NULL) {
        *out_len = table->len;
    }
    return table->rows;
}

const sora_showcase_stage* sora_showcase_stage_table_get(
    const sora_showcase_stage_table* table,
    int32_t key
) {
    for (size_t index = 0; index < table->len; ++index) {
        const sora_showcase_stage* row = &table->rows[index];
        if (row->id == key) {
            return row;
        }
    }
    return NULL;
}
