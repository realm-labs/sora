/* This file was generated by Sora. Do not edit manually. */

#include "vip_level.h"
#include <stdlib.h>

sora_result sora_showcase_vip_level_decode(sora_reader* reader, sora_showcase_vip_level* out) {
    *out = (sora_showcase_vip_level){0};
    {
        sora_result result = sora_reader_read_i32(reader, &out->level);
        if (result.code != SORA_OK) {
            sora_showcase_vip_level_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_showcase_resource_cost_decode(reader, &out->cost);
        if (result.code != SORA_OK) {
            sora_showcase_vip_level_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_showcase_string_array_decode(reader, &out->perks);
        if (result.code != SORA_OK) {
            sora_showcase_vip_level_free(out);
            return result;
        }
    }
    return sora_ok();
}

void sora_showcase_vip_level_free(sora_showcase_vip_level* value) {
    if (value == NULL) {
        return;
    }
    sora_showcase_resource_cost_free(&value->cost);
    sora_showcase_string_array_free(&value->perks);
    *value = (sora_showcase_vip_level){0};
}

struct sora_showcase_vip_level_table {
    sora_showcase_vip_level* rows;
    size_t len;
};

static void sora_showcase_vip_level_table_free_rows(sora_showcase_vip_level* rows, size_t len) {
    if (rows == NULL) {
        return;
    }
    for (size_t index = 0; index < len; ++index) {
        sora_showcase_vip_level_free(&rows[index]);
    }
    free(rows);
}

sora_result sora_showcase_vip_level_table_load(
    const sora_bundle* bundle,
    sora_showcase_vip_level_table** out
) {
    sora_showcase_vip_level_table* table = (sora_showcase_vip_level_table*)calloc(1, sizeof(sora_showcase_vip_level_table));
    if (table == NULL) {
        return sora_error(SORA_ERROR_OUT_OF_MEMORY, "failed to allocate table VipLevel");
    }
    sora_result result = sora_bundle_decode_table(
        bundle,
        "VipLevel",
        sizeof(sora_showcase_vip_level),
        (sora_decode_row_fn)sora_showcase_vip_level_decode,
        (sora_free_row_fn)sora_showcase_vip_level_free,
        (void**)&table->rows,
        &table->len
    );
    if (result.code != SORA_OK) {
        sora_showcase_vip_level_table_free(table);
        return result;
    }
    *out = table;
    return sora_ok();
}

void sora_showcase_vip_level_table_free(sora_showcase_vip_level_table* table) {
    if (table == NULL) {
        return;
    }
    sora_showcase_vip_level_table_free_rows(table->rows, table->len);
    free(table);
}

const sora_showcase_vip_level* sora_showcase_vip_level_table_rows(
    const sora_showcase_vip_level_table* table,
    size_t* out_len
) {
    if (out_len != NULL) {
        *out_len = table->len;
    }
    return table->rows;
}

const sora_showcase_vip_level* sora_showcase_vip_level_table_get(
    const sora_showcase_vip_level_table* table,
    int32_t key
) {
    for (size_t index = 0; index < table->len; ++index) {
        const sora_showcase_vip_level* row = &table->rows[index];
        if (row->level == key) {
            return row;
        }
    }
    return NULL;
}
