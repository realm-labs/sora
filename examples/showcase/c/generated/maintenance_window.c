/* This file was generated by Sora. Do not edit manually. */

#include "maintenance_window.h"
#include <stdlib.h>

sora_result sora_showcase_maintenance_window_decode(sora_reader* reader, sora_showcase_maintenance_window* out) {
    *out = (sora_showcase_maintenance_window){0};
    {
        sora_result result = sora_reader_read_string(reader, &out->version);
        if (result.code != SORA_OK) {
            sora_showcase_maintenance_window_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_string(reader, &out->starts_at);
        if (result.code != SORA_OK) {
            sora_showcase_maintenance_window_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_i32(reader, &out->duration_minutes);
        if (result.code != SORA_OK) {
            sora_showcase_maintenance_window_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_showcase_optional_string_decode(reader, &out->reason);
        if (result.code != SORA_OK) {
            sora_showcase_maintenance_window_free(out);
            return result;
        }
    }
    return sora_ok();
}

void sora_showcase_maintenance_window_free(sora_showcase_maintenance_window* value) {
    if (value == NULL) {
        return;
    }
    sora_string_free(&value->version);
    sora_string_free(&value->starts_at);
    sora_showcase_optional_string_free(&value->reason);
    *value = (sora_showcase_maintenance_window){0};
}

static const char* sora_showcase_maintenance_window_table_name = "MaintenanceWindow";

struct sora_showcase_maintenance_window_table {
    sora_showcase_maintenance_window* rows;
    size_t len;
};

static void sora_showcase_maintenance_window_table_free_rows(sora_showcase_maintenance_window* rows, size_t len) {
    if (rows == NULL) {
        return;
    }
    for (size_t index = 0; index < len; ++index) {
        sora_showcase_maintenance_window_free(&rows[index]);
    }
    free(rows);
}

sora_result sora_showcase_maintenance_window_table_load(
    const sora_bundle* bundle,
    sora_showcase_maintenance_window_table** out
) {
    sora_showcase_maintenance_window_table* table = (sora_showcase_maintenance_window_table*)calloc(1, sizeof(sora_showcase_maintenance_window_table));
    if (table == NULL) {
        return sora_error(SORA_ERROR_OUT_OF_MEMORY, "failed to allocate table");
    }
    sora_result result = sora_bundle_decode_table(
        bundle,
        sora_showcase_maintenance_window_table_name,
        sizeof(sora_showcase_maintenance_window),
        (sora_decode_row_fn)sora_showcase_maintenance_window_decode,
        (sora_free_row_fn)sora_showcase_maintenance_window_free,
        (void**)&table->rows,
        &table->len
    );
    if (result.code != SORA_OK) {
        sora_showcase_maintenance_window_table_free(table);
        return result;
    }
    *out = table;
    return sora_ok();
}

void sora_showcase_maintenance_window_table_free(sora_showcase_maintenance_window_table* table) {
    if (table == NULL) {
        return;
    }
    sora_showcase_maintenance_window_table_free_rows(table->rows, table->len);
    free(table);
}

const sora_showcase_maintenance_window* sora_showcase_maintenance_window_table_rows(
    const sora_showcase_maintenance_window_table* table,
    size_t* out_len
) {
    if (out_len != NULL) {
        *out_len = table->len;
    }
    return table->rows;
}
