/* This file was generated by Sora. Do not edit manually. */

#include "drop_entry.h"
#include <stdlib.h>

sora_result sora_showcase_drop_entry_decode(sora_reader* reader, sora_showcase_drop_entry* out) {
    *out = (sora_showcase_drop_entry){0};
    {
        sora_result result = sora_reader_read_i32(reader, &out->group_id);
        if (result.code != SORA_OK) {
            sora_showcase_drop_entry_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_i32(reader, &out->seq);
        if (result.code != SORA_OK) {
            sora_showcase_drop_entry_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_i32(reader, &out->item_id);
        if (result.code != SORA_OK) {
            sora_showcase_drop_entry_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_i32(reader, &out->count);
        if (result.code != SORA_OK) {
            sora_showcase_drop_entry_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_f32(reader, &out->weight);
        if (result.code != SORA_OK) {
            sora_showcase_drop_entry_free(out);
            return result;
        }
    }
    return sora_ok();
}

void sora_showcase_drop_entry_free(sora_showcase_drop_entry* value) {
    if (value == NULL) {
        return;
    }
    *value = (sora_showcase_drop_entry){0};
}

static const char* sora_showcase_drop_entry_table_name = "DropEntry";

struct sora_showcase_drop_entry_table {
    sora_showcase_drop_entry* rows;
    size_t len;
};

static void sora_showcase_drop_entry_table_free_rows(sora_showcase_drop_entry* rows, size_t len) {
    if (rows == NULL) {
        return;
    }
    for (size_t index = 0; index < len; ++index) {
        sora_showcase_drop_entry_free(&rows[index]);
    }
    free(rows);
}

sora_result sora_showcase_drop_entry_table_load(
    const sora_bundle* bundle,
    sora_showcase_drop_entry_table** out
) {
    sora_showcase_drop_entry_table* table = (sora_showcase_drop_entry_table*)calloc(1, sizeof(sora_showcase_drop_entry_table));
    if (table == NULL) {
        return sora_error(SORA_ERROR_OUT_OF_MEMORY, "failed to allocate table");
    }
    sora_result result = sora_bundle_decode_table(
        bundle,
        sora_showcase_drop_entry_table_name,
        sizeof(sora_showcase_drop_entry),
        (sora_decode_row_fn)sora_showcase_drop_entry_decode,
        (sora_free_row_fn)sora_showcase_drop_entry_free,
        (void**)&table->rows,
        &table->len
    );
    if (result.code != SORA_OK) {
        sora_showcase_drop_entry_table_free(table);
        return result;
    }
    *out = table;
    return sora_ok();
}

void sora_showcase_drop_entry_table_free(sora_showcase_drop_entry_table* table) {
    if (table == NULL) {
        return;
    }
    sora_showcase_drop_entry_table_free_rows(table->rows, table->len);
    free(table);
}

const sora_showcase_drop_entry* sora_showcase_drop_entry_table_rows(
    const sora_showcase_drop_entry_table* table,
    size_t* out_len
) {
    if (out_len != NULL) {
        *out_len = table->len;
    }
    return table->rows;
}
