/* This file was generated by Sora. Do not edit manually. */

#include "level_exp.h"
#include <stdlib.h>

sora_result sora_showcase_level_exp_decode(sora_reader* reader, sora_showcase_level_exp* out) {
    *out = (sora_showcase_level_exp){0};
    {
        sora_result result = sora_reader_read_i32(reader, &out->level);
        if (result.code != SORA_OK) {
            sora_showcase_level_exp_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_i64(reader, &out->exp);
        if (result.code != SORA_OK) {
            sora_showcase_level_exp_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_showcase_optional_string_decode(reader, &out->unlock_feature);
        if (result.code != SORA_OK) {
            sora_showcase_level_exp_free(out);
            return result;
        }
    }
    return sora_ok();
}

void sora_showcase_level_exp_free(sora_showcase_level_exp* value) {
    if (value == NULL) {
        return;
    }
    sora_showcase_optional_string_free(&value->unlock_feature);
    *value = (sora_showcase_level_exp){0};
}

static const char* sora_showcase_level_exp_table_name = "LevelExp";

struct sora_showcase_level_exp_table {
    sora_showcase_level_exp* rows;
    size_t len;
};

static void sora_showcase_level_exp_table_free_rows(sora_showcase_level_exp* rows, size_t len) {
    if (rows == NULL) {
        return;
    }
    for (size_t index = 0; index < len; ++index) {
        sora_showcase_level_exp_free(&rows[index]);
    }
    free(rows);
}

sora_result sora_showcase_level_exp_table_load(
    const sora_bundle* bundle,
    sora_showcase_level_exp_table** out
) {
    sora_showcase_level_exp_table* table = (sora_showcase_level_exp_table*)calloc(1, sizeof(sora_showcase_level_exp_table));
    if (table == NULL) {
        return sora_error(SORA_ERROR_OUT_OF_MEMORY, "failed to allocate table");
    }
    sora_result result = sora_bundle_decode_table(
        bundle,
        sora_showcase_level_exp_table_name,
        sizeof(sora_showcase_level_exp),
        (sora_decode_row_fn)sora_showcase_level_exp_decode,
        (sora_free_row_fn)sora_showcase_level_exp_free,
        (void**)&table->rows,
        &table->len
    );
    if (result.code != SORA_OK) {
        sora_showcase_level_exp_table_free(table);
        return result;
    }
    *out = table;
    return sora_ok();
}

void sora_showcase_level_exp_table_free(sora_showcase_level_exp_table* table) {
    if (table == NULL) {
        return;
    }
    sora_showcase_level_exp_table_free_rows(table->rows, table->len);
    free(table);
}

const sora_showcase_level_exp* sora_showcase_level_exp_table_rows(
    const sora_showcase_level_exp_table* table,
    size_t* out_len
) {
    if (out_len != NULL) {
        *out_len = table->len;
    }
    return table->rows;
}

const sora_showcase_level_exp* sora_showcase_level_exp_table_get(
    const sora_showcase_level_exp_table* table,
    int32_t key
) {
    for (size_t index = 0; index < table->len; ++index) {
        const sora_showcase_level_exp* row = &table->rows[index];
        if (row->level == key) {
            return row;
        }
    }
    return NULL;
}
