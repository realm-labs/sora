/* This file was generated by Sora. Do not edit manually. */

#include "reward_bundle.h"

sora_result sora_showcase_reward_bundle_decode(sora_reader* reader, sora_showcase_reward_bundle* out) {
    *out = (sora_showcase_reward_bundle){0};
    {
        sora_result result = sora_showcase_resource_cost_decode(reader, &out->cost);
        if (result.code != SORA_OK) {
            sora_showcase_reward_bundle_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_i32(reader, &out->weight);
        if (result.code != SORA_OK) {
            sora_showcase_reward_bundle_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_showcase_string_array_decode(reader, &out->labels);
        if (result.code != SORA_OK) {
            sora_showcase_reward_bundle_free(out);
            return result;
        }
    }
    return sora_ok();
}

void sora_showcase_reward_bundle_free(sora_showcase_reward_bundle* value) {
    if (value == NULL) {
        return;
    }
    sora_showcase_resource_cost_free(&value->cost);
    sora_showcase_string_array_free(&value->labels);
    *value = (sora_showcase_reward_bundle){0};
}
