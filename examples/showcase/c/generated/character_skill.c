/* This file was generated by Sora. Do not edit manually. */

#include "character_skill.h"
#include <stdlib.h>

sora_result sora_showcase_character_skill_decode(sora_reader* reader, sora_showcase_character_skill* out) {
    *out = (sora_showcase_character_skill){0};
    {
        sora_result result = sora_reader_read_i32(reader, &out->character_id);
        if (result.code != SORA_OK) {
            sora_showcase_character_skill_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_i32(reader, &out->skill_id);
        if (result.code != SORA_OK) {
            sora_showcase_character_skill_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_i32(reader, &out->unlock_level);
        if (result.code != SORA_OK) {
            sora_showcase_character_skill_free(out);
            return result;
        }
    }
    return sora_ok();
}

void sora_showcase_character_skill_free(sora_showcase_character_skill* value) {
    if (value == NULL) {
        return;
    }
    *value = (sora_showcase_character_skill){0};
}

struct sora_showcase_character_skill_table {
    sora_showcase_character_skill* rows;
    size_t len;
};

static void sora_showcase_character_skill_table_free_rows(sora_showcase_character_skill* rows, size_t len) {
    if (rows == NULL) {
        return;
    }
    for (size_t index = 0; index < len; ++index) {
        sora_showcase_character_skill_free(&rows[index]);
    }
    free(rows);
}

sora_result sora_showcase_character_skill_table_load(
    const sora_bundle* bundle,
    sora_showcase_character_skill_table** out
) {
    sora_showcase_character_skill_table* table = (sora_showcase_character_skill_table*)calloc(1, sizeof(sora_showcase_character_skill_table));
    if (table == NULL) {
        return sora_error(SORA_ERROR_OUT_OF_MEMORY, "failed to allocate table CharacterSkill");
    }
    sora_result result = sora_bundle_decode_table(
        bundle,
        "CharacterSkill",
        sizeof(sora_showcase_character_skill),
        (sora_decode_row_fn)sora_showcase_character_skill_decode,
        (sora_free_row_fn)sora_showcase_character_skill_free,
        (void**)&table->rows,
        &table->len
    );
    if (result.code != SORA_OK) {
        sora_showcase_character_skill_table_free(table);
        return result;
    }
    *out = table;
    return sora_ok();
}

void sora_showcase_character_skill_table_free(sora_showcase_character_skill_table* table) {
    if (table == NULL) {
        return;
    }
    sora_showcase_character_skill_table_free_rows(table->rows, table->len);
    free(table);
}

const sora_showcase_character_skill* sora_showcase_character_skill_table_rows(
    const sora_showcase_character_skill_table* table,
    size_t* out_len
) {
    if (out_len != NULL) {
        *out_len = table->len;
    }
    return table->rows;
}
