/* This file was generated by Sora. Do not edit manually. */

#include "gacha_item.h"
#include <stdlib.h>

sora_result sora_showcase_gacha_item_decode(sora_reader* reader, sora_showcase_gacha_item* out) {
    *out = (sora_showcase_gacha_item){0};
    {
        sora_result result = sora_reader_read_i32(reader, &out->pool_id);
        if (result.code != SORA_OK) {
            sora_showcase_gacha_item_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_i32(reader, &out->item_id);
        if (result.code != SORA_OK) {
            sora_showcase_gacha_item_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_showcase_rarity_decode(reader, &out->rarity);
        if (result.code != SORA_OK) {
            sora_showcase_gacha_item_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_f32(reader, &out->weight);
        if (result.code != SORA_OK) {
            sora_showcase_gacha_item_free(out);
            return result;
        }
    }
    return sora_ok();
}

void sora_showcase_gacha_item_free(sora_showcase_gacha_item* value) {
    if (value == NULL) {
        return;
    }
    *value = (sora_showcase_gacha_item){0};
}

struct sora_showcase_gacha_item_table {
    sora_showcase_gacha_item* rows;
    size_t len;
};

static void sora_showcase_gacha_item_table_free_rows(sora_showcase_gacha_item* rows, size_t len) {
    if (rows == NULL) {
        return;
    }
    for (size_t index = 0; index < len; ++index) {
        sora_showcase_gacha_item_free(&rows[index]);
    }
    free(rows);
}

sora_result sora_showcase_gacha_item_table_load(
    const sora_bundle* bundle,
    sora_showcase_gacha_item_table** out
) {
    sora_showcase_gacha_item_table* table = (sora_showcase_gacha_item_table*)calloc(1, sizeof(sora_showcase_gacha_item_table));
    if (table == NULL) {
        return sora_error(SORA_ERROR_OUT_OF_MEMORY, "failed to allocate table GachaItem");
    }
    sora_result result = sora_bundle_decode_table(
        bundle,
        "GachaItem",
        sizeof(sora_showcase_gacha_item),
        (sora_decode_row_fn)sora_showcase_gacha_item_decode,
        (sora_free_row_fn)sora_showcase_gacha_item_free,
        (void**)&table->rows,
        &table->len
    );
    if (result.code != SORA_OK) {
        sora_showcase_gacha_item_table_free(table);
        return result;
    }
    *out = table;
    return sora_ok();
}

void sora_showcase_gacha_item_table_free(sora_showcase_gacha_item_table* table) {
    if (table == NULL) {
        return;
    }
    sora_showcase_gacha_item_table_free_rows(table->rows, table->len);
    free(table);
}

const sora_showcase_gacha_item* sora_showcase_gacha_item_table_rows(
    const sora_showcase_gacha_item_table* table,
    size_t* out_len
) {
    if (out_len != NULL) {
        *out_len = table->len;
    }
    return table->rows;
}
