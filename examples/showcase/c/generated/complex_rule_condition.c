/* This file was generated by Sora. Do not edit manually. */

#include "complex_rule_condition.h"
#include <stdlib.h>

sora_result sora_showcase_complex_rule_condition_decode(sora_reader* reader, sora_showcase_complex_rule_condition* out) {
    *out = (sora_showcase_complex_rule_condition){0};
    {
        sora_result result = sora_reader_read_i32(reader, &out->id);
        if (result.code != SORA_OK) {
            sora_showcase_complex_rule_condition_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_i32(reader, &out->rule_id);
        if (result.code != SORA_OK) {
            sora_showcase_complex_rule_condition_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_showcase_event_condition_decode(reader, &out->value);
        if (result.code != SORA_OK) {
            sora_showcase_complex_rule_condition_free(out);
            return result;
        }
    }
    return sora_ok();
}

void sora_showcase_complex_rule_condition_free(sora_showcase_complex_rule_condition* value) {
    if (value == NULL) {
        return;
    }
    sora_showcase_event_condition_free(&value->value);
    *value = (sora_showcase_complex_rule_condition){0};
}

static const char* sora_showcase_complex_rule_condition_table_name = "ComplexRuleCondition";

struct sora_showcase_complex_rule_condition_table {
    sora_showcase_complex_rule_condition* rows;
    size_t len;
};

static void sora_showcase_complex_rule_condition_table_free_rows(sora_showcase_complex_rule_condition* rows, size_t len) {
    if (rows == NULL) {
        return;
    }
    for (size_t index = 0; index < len; ++index) {
        sora_showcase_complex_rule_condition_free(&rows[index]);
    }
    free(rows);
}

sora_result sora_showcase_complex_rule_condition_table_load(
    const sora_bundle* bundle,
    sora_showcase_complex_rule_condition_table** out
) {
    sora_showcase_complex_rule_condition_table* table = (sora_showcase_complex_rule_condition_table*)calloc(1, sizeof(sora_showcase_complex_rule_condition_table));
    if (table == NULL) {
        return sora_error(SORA_ERROR_OUT_OF_MEMORY, "failed to allocate table");
    }
    sora_result result = sora_bundle_decode_table(
        bundle,
        sora_showcase_complex_rule_condition_table_name,
        sizeof(sora_showcase_complex_rule_condition),
        (sora_decode_row_fn)sora_showcase_complex_rule_condition_decode,
        (sora_free_row_fn)sora_showcase_complex_rule_condition_free,
        (void**)&table->rows,
        &table->len
    );
    if (result.code != SORA_OK) {
        sora_showcase_complex_rule_condition_table_free(table);
        return result;
    }
    *out = table;
    return sora_ok();
}

void sora_showcase_complex_rule_condition_table_free(sora_showcase_complex_rule_condition_table* table) {
    if (table == NULL) {
        return;
    }
    sora_showcase_complex_rule_condition_table_free_rows(table->rows, table->len);
    free(table);
}

const sora_showcase_complex_rule_condition* sora_showcase_complex_rule_condition_table_rows(
    const sora_showcase_complex_rule_condition_table* table,
    size_t* out_len
) {
    if (out_len != NULL) {
        *out_len = table->len;
    }
    return table->rows;
}

const sora_showcase_complex_rule_condition* sora_showcase_complex_rule_condition_table_get(
    const sora_showcase_complex_rule_condition_table* table,
    int32_t key
) {
    for (size_t index = 0; index < table->len; ++index) {
        const sora_showcase_complex_rule_condition* row = &table->rows[index];
        if (row->id == key) {
            return row;
        }
    }
    return NULL;
}
