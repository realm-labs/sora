/* This file was generated by Sora. Do not edit manually. */

#include "localization.h"
#include <stdlib.h>

sora_result sora_showcase_localization_decode(sora_reader* reader, sora_showcase_localization* out) {
    *out = (sora_showcase_localization){0};
    {
        sora_result result = sora_reader_read_string(reader, &out->key);
        if (result.code != SORA_OK) {
            sora_showcase_localization_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_string(reader, &out->zh_cn);
        if (result.code != SORA_OK) {
            sora_showcase_localization_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_string(reader, &out->en_us);
        if (result.code != SORA_OK) {
            sora_showcase_localization_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_showcase_optional_string_decode(reader, &out->note);
        if (result.code != SORA_OK) {
            sora_showcase_localization_free(out);
            return result;
        }
    }
    return sora_ok();
}

void sora_showcase_localization_free(sora_showcase_localization* value) {
    if (value == NULL) {
        return;
    }
    sora_string_free(&value->key);
    sora_string_free(&value->zh_cn);
    sora_string_free(&value->en_us);
    sora_showcase_optional_string_free(&value->note);
    *value = (sora_showcase_localization){0};
}

struct sora_showcase_localization_table {
    sora_showcase_localization* rows;
    size_t len;
};

static void sora_showcase_localization_table_free_rows(sora_showcase_localization* rows, size_t len) {
    if (rows == NULL) {
        return;
    }
    for (size_t index = 0; index < len; ++index) {
        sora_showcase_localization_free(&rows[index]);
    }
    free(rows);
}

sora_result sora_showcase_localization_table_load(
    const sora_bundle* bundle,
    sora_showcase_localization_table** out
) {
    sora_showcase_localization_table* table = (sora_showcase_localization_table*)calloc(1, sizeof(sora_showcase_localization_table));
    if (table == NULL) {
        return sora_error(SORA_ERROR_OUT_OF_MEMORY, "failed to allocate table Localization");
    }
    sora_result result = sora_bundle_decode_table(
        bundle,
        "Localization",
        sizeof(sora_showcase_localization),
        (sora_decode_row_fn)sora_showcase_localization_decode,
        (sora_free_row_fn)sora_showcase_localization_free,
        (void**)&table->rows,
        &table->len
    );
    if (result.code != SORA_OK) {
        sora_showcase_localization_table_free(table);
        return result;
    }
    *out = table;
    return sora_ok();
}

void sora_showcase_localization_table_free(sora_showcase_localization_table* table) {
    if (table == NULL) {
        return;
    }
    sora_showcase_localization_table_free_rows(table->rows, table->len);
    free(table);
}

const sora_showcase_localization* sora_showcase_localization_table_rows(
    const sora_showcase_localization_table* table,
    size_t* out_len
) {
    if (out_len != NULL) {
        *out_len = table->len;
    }
    return table->rows;
}

const sora_showcase_localization* sora_showcase_localization_table_get(
    const sora_showcase_localization_table* table,
    const sora_string* key
) {
    for (size_t index = 0; index < table->len; ++index) {
        const sora_showcase_localization* row = &table->rows[index];
        if (sora_string_equal(&row->key, key)) {
            return row;
        }
    }
    return NULL;
}
