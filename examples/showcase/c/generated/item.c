/* This file was generated by Sora. Do not edit manually. */

#include "item.h"
#include <stdlib.h>

sora_result sora_showcase_item_decode(sora_reader* reader, sora_showcase_item* out) {
    *out = (sora_showcase_item){0};
    {
        sora_result result = sora_reader_read_i32(reader, &out->id);
        if (result.code != SORA_OK) {
            sora_showcase_item_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_string(reader, &out->name);
        if (result.code != SORA_OK) {
            sora_showcase_item_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_showcase_item_type_decode(reader, &out->item_type);
        if (result.code != SORA_OK) {
            sora_showcase_item_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_i32(reader, &out->max_stack);
        if (result.code != SORA_OK) {
            sora_showcase_item_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_showcase_resource_cost_decode(reader, &out->price);
        if (result.code != SORA_OK) {
            sora_showcase_item_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_showcase_string_array_decode(reader, &out->tags);
        if (result.code != SORA_OK) {
            sora_showcase_item_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_showcase_string_i32_map_decode(reader, &out->attributes);
        if (result.code != SORA_OK) {
            sora_showcase_item_free(out);
            return result;
        }
    }
    return sora_ok();
}

void sora_showcase_item_free(sora_showcase_item* value) {
    if (value == NULL) {
        return;
    }
    sora_string_free(&value->name);
    sora_showcase_resource_cost_free(&value->price);
    sora_showcase_string_array_free(&value->tags);
    sora_showcase_string_i32_map_free(&value->attributes);
    *value = (sora_showcase_item){0};
}

static const char* sora_showcase_item_table_name = "Item";

struct sora_showcase_item_table {
    sora_showcase_item* rows;
    size_t len;
};

static void sora_showcase_item_table_free_rows(sora_showcase_item* rows, size_t len) {
    if (rows == NULL) {
        return;
    }
    for (size_t index = 0; index < len; ++index) {
        sora_showcase_item_free(&rows[index]);
    }
    free(rows);
}

sora_result sora_showcase_item_table_load(
    const sora_bundle* bundle,
    sora_showcase_item_table** out
) {
    sora_showcase_item_table* table = (sora_showcase_item_table*)calloc(1, sizeof(sora_showcase_item_table));
    if (table == NULL) {
        return sora_error(SORA_ERROR_OUT_OF_MEMORY, "failed to allocate table");
    }
    sora_result result = sora_bundle_decode_table(
        bundle,
        sora_showcase_item_table_name,
        sizeof(sora_showcase_item),
        (sora_decode_row_fn)sora_showcase_item_decode,
        (sora_free_row_fn)sora_showcase_item_free,
        (void**)&table->rows,
        &table->len
    );
    if (result.code != SORA_OK) {
        sora_showcase_item_table_free(table);
        return result;
    }
    *out = table;
    return sora_ok();
}

void sora_showcase_item_table_free(sora_showcase_item_table* table) {
    if (table == NULL) {
        return;
    }
    sora_showcase_item_table_free_rows(table->rows, table->len);
    free(table);
}

const sora_showcase_item* sora_showcase_item_table_rows(
    const sora_showcase_item_table* table,
    size_t* out_len
) {
    if (out_len != NULL) {
        *out_len = table->len;
    }
    return table->rows;
}

const sora_showcase_item* sora_showcase_item_table_get(
    const sora_showcase_item_table* table,
    int32_t key
) {
    for (size_t index = 0; index < table->len; ++index) {
        const sora_showcase_item* row = &table->rows[index];
        if (row->id == key) {
            return row;
        }
    }
    return NULL;
}

const sora_showcase_item* sora_showcase_item_table_get_by_name(
    const sora_showcase_item_table* table,
    const sora_string* name
) {
    for (size_t index = 0; index < table->len; ++index) {
        const sora_showcase_item* row = &table->rows[index];
        if (sora_string_equal(&row->name, name)) {
            return row;
        }
    }
    return NULL;
}

size_t sora_showcase_item_table_find_by_item_type(
    const sora_showcase_item_table* table,
    sora_showcase_item_type item_type,
    const sora_showcase_item** out,
    size_t capacity
) {
    size_t count = 0;
    for (size_t index = 0; index < table->len; ++index) {
        const sora_showcase_item* row = &table->rows[index];
        if (row->item_type == item_type) {
            if (out != NULL && count < capacity) {
                out[count] = row;
            }
            ++count;
        }
    }
    return count;
}
