/* This file was generated by Sora. Do not edit manually. */

#include "item.h"

sora_result sora_showcase_item_decode(sora_reader* reader, sora_showcase_item* out) {
    *out = (sora_showcase_item){0};
    {
        sora_result result = sora_reader_read_i32(reader, &out->id);
        if (result.code != SORA_OK) {
            sora_showcase_item_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_string(reader, &out->name);
        if (result.code != SORA_OK) {
            sora_showcase_item_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_showcase_item_type_decode(reader, &out->item_type);
        if (result.code != SORA_OK) {
            sora_showcase_item_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_i32(reader, &out->max_stack);
        if (result.code != SORA_OK) {
            sora_showcase_item_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_showcase_resource_cost_decode(reader, &out->price);
        if (result.code != SORA_OK) {
            sora_showcase_item_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_showcase_string_array_decode(reader, &out->tags);
        if (result.code != SORA_OK) {
            sora_showcase_item_free(out);
            return result;
        }
    }
    return sora_ok();
}

void sora_showcase_item_free(sora_showcase_item* value) {
    if (value == NULL) {
        return;
    }
    sora_string_free(&value->name);
    sora_showcase_resource_cost_free(&value->price);
    sora_showcase_string_array_free(&value->tags);
    *value = (sora_showcase_item){0};
}
