/* This file was generated by Sora. Do not edit manually. */

#include "character.h"

sora_result sora_showcase_character_decode(sora_reader* reader, sora_showcase_character* out) {
    *out = (sora_showcase_character){0};
    {
        sora_result result = sora_reader_read_i32(reader, &out->id);
        if (result.code != SORA_OK) {
            sora_showcase_character_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_string(reader, &out->name);
        if (result.code != SORA_OK) {
            sora_showcase_character_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_showcase_rarity_decode(reader, &out->rarity);
        if (result.code != SORA_OK) {
            sora_showcase_character_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_i32(reader, &out->base_level);
        if (result.code != SORA_OK) {
            sora_showcase_character_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_reader_read_i32(reader, &out->base_skill);
        if (result.code != SORA_OK) {
            sora_showcase_character_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_showcase_i32_array_decode(reader, &out->starter_items);
        if (result.code != SORA_OK) {
            sora_showcase_character_free(out);
            return result;
        }
    }
    {
        sora_result result = sora_showcase_vec3_decode(reader, &out->spawn_pos);
        if (result.code != SORA_OK) {
            sora_showcase_character_free(out);
            return result;
        }
    }
    return sora_ok();
}

void sora_showcase_character_free(sora_showcase_character* value) {
    if (value == NULL) {
        return;
    }
    sora_string_free(&value->name);
    sora_showcase_i32_array_free(&value->starter_items);
    sora_showcase_vec3_free(&value->spawn_pos);
    *value = (sora_showcase_character){0};
}
