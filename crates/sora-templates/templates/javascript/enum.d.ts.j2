// This file was generated by Sora. Do not edit manually.

import type { SoraReader, SoraValue, TextKey } from "./sora_runtime{{ options.import_ext }}";

{%- if enum.comment %}
/** {{ enum.comment }} */
{%- endif %}
export type {{ enum.name }} =
{%- if options.enum_is_integer %}
{%- for value in enum.values %}
    | {{ value.id }}
{%- else %}
    never
{%- endfor %};
{%- else %}
{%- for value in enum.values %}
    | "{{ value.name }}"
{%- else %}
    never
{%- endfor %};
{%- endif %}

export declare const {{ enum.name }}: {
{%- for value in enum.values %}
{%- if value.comment %}
    /** {{ value.comment }} */
{%- endif %}
{%- if options.enum_is_integer %}
    readonly {{ value.name }}: {{ value.id }};
{%- else %}
    readonly {{ value.name }}: "{{ value.name }}";
{%- endif %}
{%- endfor %}
};

export declare function decode{{ enum.name }}(reader: SoraReader): {{ enum.name }};
export declare function decode{{ enum.name }}Value(value: SoraValue): {{ enum.name }};
export declare function collect{{ enum.name }}TextKeys(value: {{ enum.name }}, out: TextKey[]): void;
