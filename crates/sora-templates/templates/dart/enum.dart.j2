// This file was generated by Sora. Do not edit manually.

import 'runtime.dart';

{%- if enum.comment %}
/// {{ enum.comment }}
{%- endif %}
enum {{ enum.pascal_name }} {
{%- for value in enum.values %}
{%- if value.comment %}
  /// {{ value.comment }}
{%- endif %}
  {{ value.name }}({{ value.id }}){% if loop.last %};{% else %},{% endif %}
{%- endfor %}

  const {{ enum.pascal_name }}(this.id);

  final int id;

  static {{ enum.pascal_name }} decode(SoraValue value) {
    switch (value.asString()) {
{%- for value in enum.values %}
      case '{{ value.raw_name }}':
        return {{ enum.pascal_name }}.{{ value.name }};
{%- endfor %}
      default:
        throw SoraReadException('invalid enum value `${value.asString()}` for {{ enum.pascal_name }}');
    }
  }
}
