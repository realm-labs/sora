// This file was generated by Sora. Do not edit manually.

{%- if enum.comment %}
/// {{ enum.comment }}
{%- endif %}
#[repr(u32)]
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, serde::Serialize, serde::Deserialize)]
pub enum {{ enum.name }} {
{%- for value in enum.values %}
{%- if value.comment %}
    /// {{ value.comment }}
{%- endif %}
    {{ value.name }} = {{ value.id }},
{%- endfor %}
}

impl {{ enum.root_path }}::runtime::SoraDecode for {{ enum.name }} {
    fn decode(reader: &mut {{ enum.root_path }}::runtime::SoraReader<'_>) -> Result<Self, {{ enum.root_path }}::runtime::SoraReadError> {
        match reader.read_var_u32()? {
{%- for value in enum.values %}
            {{ value.id }} => Ok(Self::{{ value.name }}),
{%- endfor %}
            value => Err({{ enum.root_path }}::runtime::SoraReadError::new(format!("invalid enum id {} for {{ enum.name }}", value))),
        }
    }
}
