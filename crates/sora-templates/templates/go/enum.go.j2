// This file was generated by Sora. Do not edit manually.

package {{ package }}

import "fmt"

type {{ enum.name }} int32

const (
{%- for value in enum.values %}
    {{ enum.name }}{{ value.name }} {{ enum.name }} = {{ value.id }}
{%- endfor %}
)

func decode{{ enum.name }}(reader *SoraReader) ({{ enum.name }}, error) {
    id, err := reader.ReadUInt32()
    if err != nil {
        return 0, err
    }
    switch id {
{%- for value in enum.values %}
    case {{ value.id }}:
        return {{ enum.name }}{{ value.name }}, nil
{%- endfor %}
    default:
        return 0, fmt.Errorf("invalid enum id %d for {{ enum.name }}", id)
    }
}

func decode{{ enum.name }}Value(value SoraValue) ({{ enum.name }}, error) {
    name, err := value.AsString()
    if err != nil {
        return 0, err
    }
    switch name {
{%- for value in enum.values %}
    case "{{ value.name }}":
        return {{ enum.name }}{{ value.name }}, nil
{%- endfor %}
    default:
        return 0, fmt.Errorf("invalid enum value %s for {{ enum.name }}", name)
    }
}
